{
  "items": [
    {
      "inspection_code": "W-REPLAY-NET-LINK-01",
      "item_id": 90029,
      "application_id": 225,
      "application_type_id": 20,
      "application_type_name": "WINDOWS",
      "application_name": "A",
      "application_family_name": "A",
      "threshold_list": [
        {
          "name": "min_up_physical_nic_count",
          "value1": "1"
        },
        {
          "name": "failure_keywords",
          "value1": ""
        }
      ],
      "check_script": "# -*- coding: utf-8 -*-\n\nimport re\n\nfrom .common._base import BaseCheck\n\n\nNETWORK_LINK_STATUS_COMMAND = (\n    \"Get-NetAdapter -Physical | Select-Object Name, InterfaceDescription, Status, LinkSpeed | Format-Table -AutoSize\"\n)\n\nSTATUS_MAP = {\n    'up': 'Up',\n    'down': 'Down',\n    'unknown': 'Unknown',\n    'not present': 'Not Present',\n}\n\n\ndef _normalize_status(value):\n    return STATUS_MAP.get(str(value).strip().lower(), str(value).strip())\n\n\ndef _parse_adapter_line(line):\n    parts = [part.strip() for part in re.split(r'\\s{2,}', line.strip()) if part.strip()]\n    if not parts:\n        return None\n\n    status_index = None\n    for index, part in enumerate(parts):\n        if part.lower() in STATUS_MAP:\n            status_index = index\n            break\n\n    if status_index is None:\n        return None\n\n    name = parts[0]\n    interface_description = ''\n    if status_index > 1:\n        interface_description = ' '.join(parts[1:status_index])\n\n    status = _normalize_status(parts[status_index])\n    link_speed = parts[status_index + 1] if len(parts) > status_index + 1 else ''\n\n    return {\n        'name': name,\n        'interface_description': interface_description,\n        'status': status,\n        'link_speed': link_speed,\n    }\n\n\nclass Check(BaseCheck):\n    USE_HOST_CONNECTION = True\n    CONNECTION_METHOD = 'winrm'\n    WINRM_SHELL = 'powershell'\n\n    def run(self):\n        min_up_physical_nic_count = self.get_threshold_var('min_up_physical_nic_count', default=1, value_type='int')\n        failure_keywords_raw = self.get_threshold_var('failure_keywords', default='', value_type='str')\n\n        rc, out, err = self._run_ps(NETWORK_LINK_STATUS_COMMAND)\n\n        if self._is_connection_error(rc, err):\n            return self.fail(\n                '호스트 연결 실패',\n                message=(err or 'WinRM 연결 확인에 실패했습니다.').strip(),\n                stderr=(err or '').strip(),\n            )\n\n        if self._is_not_applicable(rc, err):\n            return self.not_applicable(\n                'WinRM 실행 환경을 사용할 수 없습니다.',\n                raw_output=(err or '').strip(),\n            )\n\n        if rc != 0:\n            return self.fail(\n                '점검 명령 실행 실패',\n                message='Windows 네트워크 링크 상태 점검 명령 실행에 실패했습니다.',\n                stdout=(out or '').strip(),\n                stderr=(err or '').strip(),\n            )\n\n        text = (out or '').strip()\n        if not text:\n            return self.fail(\n                '물리 NIC 정보 없음',\n                message='물리 NIC 링크 상태 결과가 비어 있습니다.',\n                stdout='',\n                stderr=(err or '').strip(),\n            )\n\n        failure_keywords = [\n            keyword.strip()\n            for keyword in failure_keywords_raw.split(',')\n            if keyword.strip()\n        ]\n        matched_failure_keywords = [\n            keyword for keyword in failure_keywords if keyword.lower() in text.lower()\n        ]\n        if matched_failure_keywords:\n            return self.fail(\n                '물리 NIC 실패 키워드 감지',\n                message='물리 NIC 링크 상태 결과에서 실패 키워드가 확인되었습니다.',\n                stdout=text,\n                stderr=(err or '').strip(),\n            )\n\n        lines = [line.rstrip() for line in text.splitlines() if line.strip()]\n        adapters = []\n        for line in lines:\n            stripped = line.strip()\n            if stripped.startswith('Name') and 'Status' in stripped:\n                continue\n            if stripped.startswith('----') or stripped.startswith('-----------'):\n                continue\n            adapter = _parse_adapter_line(stripped)\n            if adapter:\n                adapters.append(adapter)\n\n        if not adapters:\n            return self.fail(\n                '물리 NIC 파싱 실패',\n                message='물리 NIC 링크 상태 테이블을 해석하지 못했습니다.',\n                stdout=text,\n                stderr=(err or '').strip(),\n            )\n\n        up_adapters = [adapter for adapter in adapters if adapter['status'] == 'Up']\n        down_adapters = [adapter for adapter in adapters if adapter['status'] == 'Down']\n        unknown_adapters = [adapter for adapter in adapters if adapter['status'] == 'Unknown']\n        not_present_adapters = [adapter for adapter in adapters if adapter['status'] == 'Not Present']\n\n        if len(up_adapters) < min_up_physical_nic_count:\n            return self.fail(\n                '활성 물리 NIC 부족',\n                message='Up 상태의 물리 NIC 수가 기준치 미만입니다.',\n                stdout=text,\n                stderr=(err or '').strip(),\n            )\n\n        if down_adapters:\n            return self.fail(\n                '물리 NIC Down 상태 감지',\n                message='Down 상태의 물리 NIC가 확인되었습니다.',\n                stdout=text,\n                stderr=(err or '').strip(),\n            )\n\n        if unknown_adapters:\n            return self.fail(\n                '물리 NIC Unknown 상태 감지',\n                message='상태를 식별하지 못한 물리 NIC가 확인되었습니다.',\n                stdout=text,\n                stderr=(err or '').strip(),\n            )\n\n        reasons = '물리 NIC의 링크 상태를 점검한 결과 Up 상태의 어댑터가 확인되었고 비정상 상태는 없습니다.'\n        if not_present_adapters:\n            reasons = 'Up 상태의 물리 NIC가 확인되며, 일부 물리 NIC는 현재 장치에 존재하지 않는 상태로 표시됩니다.'\n\n        return self.ok(\n            metrics={\n                'physical_nic_count': len(adapters),\n                'up_physical_nic_count': len(up_adapters),\n                'down_physical_nic_count': len(down_adapters),\n                'unknown_physical_nic_count': len(unknown_adapters),\n                'not_present_physical_nic_count': len(not_present_adapters),\n                'up_nic_names': [adapter['name'] for adapter in up_adapters],\n                'not_present_nic_names': [adapter['name'] for adapter in not_present_adapters],\n                'link_speeds': {\n                    adapter['name']: adapter['link_speed']\n                    for adapter in adapters\n                },\n                'matched_failure_keywords': matched_failure_keywords,\n            },\n            thresholds={\n                'min_up_physical_nic_count': min_up_physical_nic_count,\n                'failure_keywords': failure_keywords,\n            },\n            reasons=reasons,\n            message='Windows 네트워크 링크 상태 점검이 정상 수행되었습니다.',\n        )\n\n\nCHECK_CLASS = Check\n",
      "check_script_lines": [
        "# -*- coding: utf-8 -*-",
        "",
        "import re",
        "",
        "from .common._base import BaseCheck",
        "",
        "",
        "NETWORK_LINK_STATUS_COMMAND = (",
        "    \"Get-NetAdapter -Physical | Select-Object Name, InterfaceDescription, Status, LinkSpeed | Format-Table -AutoSize\"",
        ")",
        "",
        "STATUS_MAP = {",
        "    'up': 'Up',",
        "    'down': 'Down',",
        "    'unknown': 'Unknown',",
        "    'not present': 'Not Present',",
        "}",
        "",
        "",
        "def _normalize_status(value):",
        "    return STATUS_MAP.get(str(value).strip().lower(), str(value).strip())",
        "",
        "",
        "def _parse_adapter_line(line):",
        "    parts = [part.strip() for part in re.split(r'\\s{2,}', line.strip()) if part.strip()]",
        "    if not parts:",
        "        return None",
        "",
        "    status_index = None",
        "    for index, part in enumerate(parts):",
        "        if part.lower() in STATUS_MAP:",
        "            status_index = index",
        "            break",
        "",
        "    if status_index is None:",
        "        return None",
        "",
        "    name = parts[0]",
        "    interface_description = ''",
        "    if status_index > 1:",
        "        interface_description = ' '.join(parts[1:status_index])",
        "",
        "    status = _normalize_status(parts[status_index])",
        "    link_speed = parts[status_index + 1] if len(parts) > status_index + 1 else ''",
        "",
        "    return {",
        "        'name': name,",
        "        'interface_description': interface_description,",
        "        'status': status,",
        "        'link_speed': link_speed,",
        "    }",
        "",
        "",
        "class Check(BaseCheck):",
        "    USE_HOST_CONNECTION = True",
        "    CONNECTION_METHOD = 'winrm'",
        "    WINRM_SHELL = 'powershell'",
        "",
        "    def run(self):",
        "        min_up_physical_nic_count = self.get_threshold_var('min_up_physical_nic_count', default=1, value_type='int')",
        "        failure_keywords_raw = self.get_threshold_var('failure_keywords', default='', value_type='str')",
        "",
        "        rc, out, err = self._run_ps(NETWORK_LINK_STATUS_COMMAND)",
        "",
        "        if self._is_connection_error(rc, err):",
        "            return self.fail(",
        "                '호스트 연결 실패',",
        "                message=(err or 'WinRM 연결 확인에 실패했습니다.').strip(),",
        "                stderr=(err or '').strip(),",
        "            )",
        "",
        "        if self._is_not_applicable(rc, err):",
        "            return self.not_applicable(",
        "                'WinRM 실행 환경을 사용할 수 없습니다.',",
        "                raw_output=(err or '').strip(),",
        "            )",
        "",
        "        if rc != 0:",
        "            return self.fail(",
        "                '점검 명령 실행 실패',",
        "                message='Windows 네트워크 링크 상태 점검 명령 실행에 실패했습니다.',",
        "                stdout=(out or '').strip(),",
        "                stderr=(err or '').strip(),",
        "            )",
        "",
        "        text = (out or '').strip()",
        "        if not text:",
        "            return self.fail(",
        "                '물리 NIC 정보 없음',",
        "                message='물리 NIC 링크 상태 결과가 비어 있습니다.',",
        "                stdout='',",
        "                stderr=(err or '').strip(),",
        "            )",
        "",
        "        failure_keywords = [",
        "            keyword.strip()",
        "            for keyword in failure_keywords_raw.split(',')",
        "            if keyword.strip()",
        "        ]",
        "        matched_failure_keywords = [",
        "            keyword for keyword in failure_keywords if keyword.lower() in text.lower()",
        "        ]",
        "        if matched_failure_keywords:",
        "            return self.fail(",
        "                '물리 NIC 실패 키워드 감지',",
        "                message='물리 NIC 링크 상태 결과에서 실패 키워드가 확인되었습니다.',",
        "                stdout=text,",
        "                stderr=(err or '').strip(),",
        "            )",
        "",
        "        lines = [line.rstrip() for line in text.splitlines() if line.strip()]",
        "        adapters = []",
        "        for line in lines:",
        "            stripped = line.strip()",
        "            if stripped.startswith('Name') and 'Status' in stripped:",
        "                continue",
        "            if stripped.startswith('----') or stripped.startswith('-----------'):",
        "                continue",
        "            adapter = _parse_adapter_line(stripped)",
        "            if adapter:",
        "                adapters.append(adapter)",
        "",
        "        if not adapters:",
        "            return self.fail(",
        "                '물리 NIC 파싱 실패',",
        "                message='물리 NIC 링크 상태 테이블을 해석하지 못했습니다.',",
        "                stdout=text,",
        "                stderr=(err or '').strip(),",
        "            )",
        "",
        "        up_adapters = [adapter for adapter in adapters if adapter['status'] == 'Up']",
        "        down_adapters = [adapter for adapter in adapters if adapter['status'] == 'Down']",
        "        unknown_adapters = [adapter for adapter in adapters if adapter['status'] == 'Unknown']",
        "        not_present_adapters = [adapter for adapter in adapters if adapter['status'] == 'Not Present']",
        "",
        "        if len(up_adapters) < min_up_physical_nic_count:",
        "            return self.fail(",
        "                '활성 물리 NIC 부족',",
        "                message='Up 상태의 물리 NIC 수가 기준치 미만입니다.',",
        "                stdout=text,",
        "                stderr=(err or '').strip(),",
        "            )",
        "",
        "        if down_adapters:",
        "            return self.fail(",
        "                '물리 NIC Down 상태 감지',",
        "                message='Down 상태의 물리 NIC가 확인되었습니다.',",
        "                stdout=text,",
        "                stderr=(err or '').strip(),",
        "            )",
        "",
        "        if unknown_adapters:",
        "            return self.fail(",
        "                '물리 NIC Unknown 상태 감지',",
        "                message='상태를 식별하지 못한 물리 NIC가 확인되었습니다.',",
        "                stdout=text,",
        "                stderr=(err or '').strip(),",
        "            )",
        "",
        "        reasons = '물리 NIC의 링크 상태를 점검한 결과 Up 상태의 어댑터가 확인되었고 비정상 상태는 없습니다.'",
        "        if not_present_adapters:",
        "            reasons = 'Up 상태의 물리 NIC가 확인되며, 일부 물리 NIC는 현재 장치에 존재하지 않는 상태로 표시됩니다.'",
        "",
        "        return self.ok(",
        "            metrics={",
        "                'physical_nic_count': len(adapters),",
        "                'up_physical_nic_count': len(up_adapters),",
        "                'down_physical_nic_count': len(down_adapters),",
        "                'unknown_physical_nic_count': len(unknown_adapters),",
        "                'not_present_physical_nic_count': len(not_present_adapters),",
        "                'up_nic_names': [adapter['name'] for adapter in up_adapters],",
        "                'not_present_nic_names': [adapter['name'] for adapter in not_present_adapters],",
        "                'link_speeds': {",
        "                    adapter['name']: adapter['link_speed']",
        "                    for adapter in adapters",
        "                },",
        "                'matched_failure_keywords': matched_failure_keywords,",
        "            },",
        "            thresholds={",
        "                'min_up_physical_nic_count': min_up_physical_nic_count,",
        "                'failure_keywords': failure_keywords,",
        "            },",
        "            reasons=reasons,",
        "            message='Windows 네트워크 링크 상태 점검이 정상 수행되었습니다.',",
        "        )",
        "",
        "",
        "CHECK_CLASS = Check"
      ]
    }
  ],
  "results": [
    {
      "inspection_code": "W-REPLAY-NET-LINK-01",
      "item_id": 90029,
      "application_id": 225,
      "application_type_id": 20,
      "application_type_name": "WINDOWS",
      "application_name": "A",
      "application_family_name": "A",
      "threshold_list": [
        {
          "name": "min_up_physical_nic_count",
          "value1": "1"
        },
        {
          "name": "failure_keywords",
          "value1": ""
        }
      ],
      "status": "ok",
      "metrics": {
        "physical_nic_count": 1,
        "up_physical_nic_count": 1,
        "down_physical_nic_count": 0,
        "unknown_physical_nic_count": 0,
        "not_present_physical_nic_count": 0,
        "up_nic_names": [
          "Wi-Fi"
        ],
        "not_present_nic_names": [],
        "link_speeds": {
          "Wi-Fi": "650 Mbps"
        },
        "matched_failure_keywords": []
      },
      "thresholds": {
        "min_up_physical_nic_count": 1,
        "failure_keywords": []
      },
      "reasons": "물리 NIC의 링크 상태를 점검한 결과 Up 상태의 어댑터가 확인되었고 비정상 상태는 없습니다.",
      "raw_output": "[점검 단계 1]\n - 실행 명령어: Get-NetAdapter -Physical | Select-Object Name, InterfaceDescription, Status, LinkSpeed | Format-Table -AutoSize\n - 명령 종료코드: rc=0 (정상 종료)\n - 출력 내용: Name   InterfaceDescription               Status      LinkSpeed\n----   --------------------               ------      ---------\nWi-Fi  Intel(R) Wi-Fi 6 AX201 160MHz      Up          650 Mbps\n이더넷 Realtek PCIe GbE Family Controller Not Present 0 bps",
      "raw_output_lines": [
        "[점검 단계 1]",
        " - 실행 명령어: Get-NetAdapter -Physical | Select-Object Name, InterfaceDescription, Status, LinkSpeed | Format-Table -AutoSize",
        " - 명령 종료코드: rc=0 (정상 종료)",
        " - 출력 내용: Name   InterfaceDescription               Status      LinkSpeed",
        "----   --------------------               ------      ---------",
        "Wi-Fi  Intel(R) Wi-Fi 6 AX201 160MHz      Up          650 Mbps",
        "이더넷 Realtek PCIe GbE Family Controller Not Present 0 bps"
      ],
      "message": "Windows 네트워크 링크 상태 점검이 정상 수행되었습니다."
    }
  ],
  "failed_items": []
}
