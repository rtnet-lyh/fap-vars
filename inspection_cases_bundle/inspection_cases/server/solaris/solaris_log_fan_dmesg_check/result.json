{
  "items": [
    {
      "inspection_code": "SOL-REPLAY-LOG-05",
      "item_id": 94017,
      "application_id": 417,
      "application_type_id": 40,
      "application_type_name": "SOLARIS",
      "application_name": "Solaris",
      "application_family_name": "UNIX",
      "inspection_item_name": "FAN 작동 이상 유무 점검",
      "type_name": "일상점검",
      "area_name": "상태점검",
      "category_name": "서버(OS)",
      "description": "Solaris dmesg FAN 로그에서 failed, not spinning, operational 상태를 점검합니다.",
      "inspection_command": "dmesg | grep -i 'fan|fail'",
      "is_service_affect": "N",
      "action_content": "팬 비정상 로그 개수, 첫 번째 비정상 로그, 정상 operational 로그 유무가 기대와 다른지 확인합니다.",
      "threshold_list": [
        {
          "name": "bad_log_keywords",
          "value1": "failed,not spinning,over-temperature,fail"
        },
        {
          "name": "failure_keywords",
          "value1": "장치를 찾을 수 없습니다,not found,cannot,command not found,module missing"
        }
      ],
      "check_script": "# -*- coding: utf-8 -*-\n\nfrom .common._base import BaseCheck\n\n\nLOG_COMMAND = \"dmesg | grep -i 'fan|fail'\"\n\n\nclass Check(BaseCheck):\n    USE_HOST_CONNECTION = True\n    CONNECTION_METHOD = 'paramiko'\n    PARAMIKO_AUTH_TIMEOUT_SEC = 30\n\n    def _is_become_enabled(self):\n        value = self.get_connection_value('become', default=False)\n        return str(value).strip().lower() in ('1', 'true', 'y', 'yes', 'on')\n\n    def _build_become_command(self):\n        method = str(self.get_connection_value('become_method', default='su -') or 'su -')\n        method = ' '.join(method.strip().lower().split())\n        user = str(self.get_connection_value('become_user', default='root') or 'root').strip() or 'root'\n\n        if method == 'su':\n            return 'su ' + user\n        if method == 'su -':\n            return 'su - ' + user\n        if method == 'sudo':\n            return 'sudo -u ' + user + ' -i'\n        raise ValueError(f'unsupported become_method: {method}')\n\n    def _build_paramiko_commands(self, command):\n        if not self._is_become_enabled():\n            return [command]\n\n        return [\n            {\n                'command': self._build_become_command(),\n                'timeout': 1,\n                'ignore_prompt': True,\n            },\n            {\n                'command': str(self.get_connection_value('become_password', default='') or ''),\n                'hide_command': True,\n            },\n            command,\n        ]\n\n    def _run_check_command(self, command):\n        try:\n            results = self._run_paramiko_commands(self._build_paramiko_commands(command))\n        except ValueError as exc:\n            return 1, '', str(exc)\n\n        for item in reversed(results):\n            if item.get('command') == command:\n                return item.get('rc'), item.get('stdout', ''), item.get('stderr', '')\n\n        failed_result = next((item for item in results if item.get('rc') != 0), None)\n        if failed_result:\n            return failed_result.get('rc'), failed_result.get('stdout', ''), failed_result.get('stderr', '')\n        return 1, '', 'paramiko command result not found'\n\n\n    def _split_keywords(self, raw_value):\n        return [token.strip() for token in str(raw_value or '').split(',') if token.strip()]\n\n    def _find_bad_lines(self, lines, bad_keywords):\n        bad_lines = []\n        for line in lines:\n            lowered = line.lower()\n            if any(keyword.lower() in lowered for keyword in bad_keywords):\n                bad_lines.append(line)\n        return bad_lines\n\n    def _build_log_summary(self, lines, limit=3):\n        if not lines:\n            return 'FAN 로그 없음'\n\n        summary_lines = [line.strip() for line in lines[:limit]]\n        if len(lines) > limit:\n            summary_lines.append(f'외 {len(lines) - limit}건')\n        return ' | '.join(summary_lines)\n\n    def run(self):\n        bad_log_keywords = self._split_keywords(self.get_threshold_var('bad_log_keywords', default='failed,not spinning,over-temperature,fail', value_type='str'))\n        failure_keywords = self._split_keywords(self.get_threshold_var('failure_keywords', default='', value_type='str'))\n\n        rc, out, err = self._run_check_command(LOG_COMMAND)\n        if self._is_connection_error(rc, err):\n            return self.fail(\n                '호스트 연결 실패',\n                message=(err or 'SSH 연결 확인에 실패했습니다.').strip(),\n                stderr=(err or '').strip(),\n            )\n        if rc not in (0, 1):\n            return self.fail(\n                '점검 명령 실행 실패',\n                message=(\n                    'Solaris FAN 로그 점검에 실패했습니다. '\n                    \"현재 상태: dmesg | grep -i 'fan|fail' 명령을 정상적으로 실행하지 못했습니다.\"\n                ),\n                stdout=(out or '').strip(),\n                stderr=(err or '').strip(),\n            )\n\n        text = (out or '').strip()\n        stderr_text = (err or '').strip()\n        combined_text = '\\n'.join([value for value in (text, stderr_text) if value])\n        matched_failure_keywords = [keyword for keyword in failure_keywords if keyword.lower() in combined_text.lower()]\n        if matched_failure_keywords:\n            return self.fail(\n                'FAN 로그 실패 키워드 감지',\n                message=(\n                    'Solaris FAN 로그 점검에 실패했습니다. '\n                    f'현재 상태: 출력에서 실패 키워드 {matched_failure_keywords}가 확인되었습니다.'\n                ),\n                stdout=text,\n                stderr=stderr_text,\n            )\n        if stderr_text:\n            return self.fail(\n                '점검 명령 실행 실패',\n                message=(\n                    'Solaris FAN 로그 점검에 실패했습니다. '\n                    f'현재 상태: stderr 출력이 확인되었습니다: {stderr_text}'\n                ),\n                stdout=text,\n                stderr=stderr_text,\n            )\n\n        lines = [line.strip() for line in text.splitlines() if line.strip()]\n        bad_lines = self._find_bad_lines(lines, bad_log_keywords)\n        if bad_lines:\n            return self.fail(\n                'FAN 이상 로그 감지',\n                message=(\n                    'Solaris FAN 로그 점검에 실패했습니다. '\n                    f'현재 상태: FAN 관련 로그 {len(lines)}건 중 비정상 로그 {len(bad_lines)}건이 확인되었습니다. '\n                    f'첫 로그: {bad_lines[0]}. 로그 요약: {self._build_log_summary(bad_lines)}.'\n                ),\n                stdout=text,\n                stderr=stderr_text,\n            )\n\n        return self.ok(\n            metrics={\n                'matched_log_count': len(lines),\n                'abnormal_log_count': len(bad_lines),\n                'matched_logs': lines,\n                'abnormal_logs': bad_lines,\n                'matched_failure_keywords': matched_failure_keywords,\n            },\n            thresholds={\n                'bad_log_keywords': bad_log_keywords,\n                'failure_keywords': failure_keywords,\n            },\n            reasons='FAN 관련 비정상 로그가 검출되지 않았고 stderr도 없습니다.',\n            message=(\n                f'Solaris FAN 로그가 정상입니다. 현재 상태: FAN 관련 로그 {len(lines)}건 조회, '\n                f'비정상 로그 0건, stderr 0건으로 집계되었습니다.'\n            ),\n        )\n\n\nCHECK_CLASS = Check\n",
      "check_script_lines": [
        "# -*- coding: utf-8 -*-",
        "",
        "from .common._base import BaseCheck",
        "",
        "",
        "LOG_COMMAND = \"dmesg | grep -i 'fan|fail'\"",
        "",
        "",
        "class Check(BaseCheck):",
        "    USE_HOST_CONNECTION = True",
        "    CONNECTION_METHOD = 'paramiko'",
        "    PARAMIKO_AUTH_TIMEOUT_SEC = 30",
        "",
        "    def _is_become_enabled(self):",
        "        value = self.get_connection_value('become', default=False)",
        "        return str(value).strip().lower() in ('1', 'true', 'y', 'yes', 'on')",
        "",
        "    def _build_become_command(self):",
        "        method = str(self.get_connection_value('become_method', default='su -') or 'su -')",
        "        method = ' '.join(method.strip().lower().split())",
        "        user = str(self.get_connection_value('become_user', default='root') or 'root').strip() or 'root'",
        "",
        "        if method == 'su':",
        "            return 'su ' + user",
        "        if method == 'su -':",
        "            return 'su - ' + user",
        "        if method == 'sudo':",
        "            return 'sudo -u ' + user + ' -i'",
        "        raise ValueError(f'unsupported become_method: {method}')",
        "",
        "    def _build_paramiko_commands(self, command):",
        "        if not self._is_become_enabled():",
        "            return [command]",
        "",
        "        return [",
        "            {",
        "                'command': self._build_become_command(),",
        "                'timeout': 1,",
        "                'ignore_prompt': True,",
        "            },",
        "            {",
        "                'command': str(self.get_connection_value('become_password', default='') or ''),",
        "                'hide_command': True,",
        "            },",
        "            command,",
        "        ]",
        "",
        "    def _run_check_command(self, command):",
        "        try:",
        "            results = self._run_paramiko_commands(self._build_paramiko_commands(command))",
        "        except ValueError as exc:",
        "            return 1, '', str(exc)",
        "",
        "        for item in reversed(results):",
        "            if item.get('command') == command:",
        "                return item.get('rc'), item.get('stdout', ''), item.get('stderr', '')",
        "",
        "        failed_result = next((item for item in results if item.get('rc') != 0), None)",
        "        if failed_result:",
        "            return failed_result.get('rc'), failed_result.get('stdout', ''), failed_result.get('stderr', '')",
        "        return 1, '', 'paramiko command result not found'",
        "",
        "",
        "    def _split_keywords(self, raw_value):",
        "        return [token.strip() for token in str(raw_value or '').split(',') if token.strip()]",
        "",
        "    def _find_bad_lines(self, lines, bad_keywords):",
        "        bad_lines = []",
        "        for line in lines:",
        "            lowered = line.lower()",
        "            if any(keyword.lower() in lowered for keyword in bad_keywords):",
        "                bad_lines.append(line)",
        "        return bad_lines",
        "",
        "    def _build_log_summary(self, lines, limit=3):",
        "        if not lines:",
        "            return 'FAN 로그 없음'",
        "",
        "        summary_lines = [line.strip() for line in lines[:limit]]",
        "        if len(lines) > limit:",
        "            summary_lines.append(f'외 {len(lines) - limit}건')",
        "        return ' | '.join(summary_lines)",
        "",
        "    def run(self):",
        "        bad_log_keywords = self._split_keywords(self.get_threshold_var('bad_log_keywords', default='failed,not spinning,over-temperature,fail', value_type='str'))",
        "        failure_keywords = self._split_keywords(self.get_threshold_var('failure_keywords', default='', value_type='str'))",
        "",
        "        rc, out, err = self._run_check_command(LOG_COMMAND)",
        "        if self._is_connection_error(rc, err):",
        "            return self.fail(",
        "                '호스트 연결 실패',",
        "                message=(err or 'SSH 연결 확인에 실패했습니다.').strip(),",
        "                stderr=(err or '').strip(),",
        "            )",
        "        if rc not in (0, 1):",
        "            return self.fail(",
        "                '점검 명령 실행 실패',",
        "                message=(",
        "                    'Solaris FAN 로그 점검에 실패했습니다. '",
        "                    \"현재 상태: dmesg | grep -i 'fan|fail' 명령을 정상적으로 실행하지 못했습니다.\"",
        "                ),",
        "                stdout=(out or '').strip(),",
        "                stderr=(err or '').strip(),",
        "            )",
        "",
        "        text = (out or '').strip()",
        "        stderr_text = (err or '').strip()",
        "        combined_text = '\\n'.join([value for value in (text, stderr_text) if value])",
        "        matched_failure_keywords = [keyword for keyword in failure_keywords if keyword.lower() in combined_text.lower()]",
        "        if matched_failure_keywords:",
        "            return self.fail(",
        "                'FAN 로그 실패 키워드 감지',",
        "                message=(",
        "                    'Solaris FAN 로그 점검에 실패했습니다. '",
        "                    f'현재 상태: 출력에서 실패 키워드 {matched_failure_keywords}가 확인되었습니다.'",
        "                ),",
        "                stdout=text,",
        "                stderr=stderr_text,",
        "            )",
        "        if stderr_text:",
        "            return self.fail(",
        "                '점검 명령 실행 실패',",
        "                message=(",
        "                    'Solaris FAN 로그 점검에 실패했습니다. '",
        "                    f'현재 상태: stderr 출력이 확인되었습니다: {stderr_text}'",
        "                ),",
        "                stdout=text,",
        "                stderr=stderr_text,",
        "            )",
        "",
        "        lines = [line.strip() for line in text.splitlines() if line.strip()]",
        "        bad_lines = self._find_bad_lines(lines, bad_log_keywords)",
        "        if bad_lines:",
        "            return self.fail(",
        "                'FAN 이상 로그 감지',",
        "                message=(",
        "                    'Solaris FAN 로그 점검에 실패했습니다. '",
        "                    f'현재 상태: FAN 관련 로그 {len(lines)}건 중 비정상 로그 {len(bad_lines)}건이 확인되었습니다. '",
        "                    f'첫 로그: {bad_lines[0]}. 로그 요약: {self._build_log_summary(bad_lines)}.'",
        "                ),",
        "                stdout=text,",
        "                stderr=stderr_text,",
        "            )",
        "",
        "        return self.ok(",
        "            metrics={",
        "                'matched_log_count': len(lines),",
        "                'abnormal_log_count': len(bad_lines),",
        "                'matched_logs': lines,",
        "                'abnormal_logs': bad_lines,",
        "                'matched_failure_keywords': matched_failure_keywords,",
        "            },",
        "            thresholds={",
        "                'bad_log_keywords': bad_log_keywords,",
        "                'failure_keywords': failure_keywords,",
        "            },",
        "            reasons='FAN 관련 비정상 로그가 검출되지 않았고 stderr도 없습니다.',",
        "            message=(",
        "                f'Solaris FAN 로그가 정상입니다. 현재 상태: FAN 관련 로그 {len(lines)}건 조회, '",
        "                f'비정상 로그 0건, stderr 0건으로 집계되었습니다.'",
        "            ),",
        "        )",
        "",
        "",
        "CHECK_CLASS = Check"
      ]
    }
  ],
  "results": [
    {
      "inspection_code": "SOL-REPLAY-LOG-05",
      "item_id": 94017,
      "application_id": 417,
      "application_type_id": 40,
      "application_type_name": "SOLARIS",
      "application_name": "Solaris",
      "application_family_name": "UNIX",
      "inspection_item_name": "FAN 작동 이상 유무 점검",
      "type_name": "일상점검",
      "area_name": "상태점검",
      "category_name": "서버(OS)",
      "description": "Solaris dmesg FAN 로그에서 failed, not spinning, operational 상태를 점검합니다.",
      "inspection_command": "dmesg | grep -i 'fan|fail'",
      "is_service_affect": "N",
      "action_content": "팬 비정상 로그 개수, 첫 번째 비정상 로그, 정상 operational 로그 유무가 기대와 다른지 확인합니다.",
      "threshold_list": [
        {
          "name": "bad_log_keywords",
          "value1": "failed,not spinning,over-temperature,fail"
        },
        {
          "name": "failure_keywords",
          "value1": "장치를 찾을 수 없습니다,not found,cannot,command not found,module missing"
        }
      ],
      "status": "ok",
      "metrics": {
        "matched_log_count": 0,
        "abnormal_log_count": 0,
        "matched_logs": [],
        "abnormal_logs": [],
        "matched_failure_keywords": []
      },
      "thresholds": {
        "bad_log_keywords": [
          "failed",
          "not spinning",
          "over-temperature",
          "fail"
        ],
        "failure_keywords": [
          "장치를 찾을 수 없습니다",
          "not found",
          "cannot",
          "command not found",
          "module missing"
        ]
      },
      "reasons": "FAN 관련 비정상 로그가 검출되지 않았고 stderr도 없습니다.",
      "raw_output": "[점검 단계 1]\n - 실행 명령어: su - root\n - 명령 종료코드: rc=124 (명령 시간 초과)\n - 출력 내용(stdout): Password:\n - 출력 내용(stderr): PARAMIKO_COMMAND_TIMEOUT: prompt was not received\n\n[점검 단계 2]\n - 실행 명령어: *******\n - 명령 종료코드: rc=0 (정상 종료)\n - 출력 내용: Oracle Corporation      SunOS 5.10      Generic Patch   January 2005\n\n[점검 단계 3]\n - 실행 명령어: dmesg | grep -i 'fan|fail'\n - 명령 종료코드: rc=0 (정상 종료)",
      "raw_output_lines": [
        "[점검 단계 1]",
        " - 실행 명령어: su - root",
        " - 명령 종료코드: rc=124 (명령 시간 초과)",
        " - 출력 내용(stdout): Password:",
        " - 출력 내용(stderr): PARAMIKO_COMMAND_TIMEOUT: prompt was not received",
        "",
        "[점검 단계 2]",
        " - 실행 명령어: *******",
        " - 명령 종료코드: rc=0 (정상 종료)",
        " - 출력 내용: Oracle Corporation      SunOS 5.10      Generic Patch   January 2005",
        "",
        "[점검 단계 3]",
        " - 실행 명령어: dmesg | grep -i 'fan|fail'",
        " - 명령 종료코드: rc=0 (정상 종료)"
      ],
      "message": "Solaris FAN 로그가 정상입니다. 현재 상태: FAN 관련 로그 0건 조회, 비정상 로그 0건, stderr 0건으로 집계되었습니다."
    }
  ],
  "failed_items": []
}
