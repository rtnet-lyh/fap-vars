{
  "items": [
    {
      "inspection_code": "TUTORIAL-CISCO-PARAMIKO-04",
      "item_id": 98304,
      "application_id": 80300,
      "application_type_id": 30,
      "application_type_name": "NETWORK",
      "application_name": "CISCO_IOS",
      "application_family_name": "CISCO_IOS",
      "threshold_list": [],
      "check_script": "# -*- coding: utf-8 -*-\n\nfrom .common._base import BaseCheck\n\n\nHOSTNAME_COMMAND = 'show running-config | include ^hostname'\n\n\nclass Check(BaseCheck):\n    USE_HOST_CONNECTION = True\n    CONNECTION_METHOD = 'paramiko'\n    PARAMIKO_PROFILE = 'cisco_ios'\n\n    def _run_with_enable(self, command_items):\n        enable_password = str(self.get_connection_value('en_password', '') or '')\n        if not enable_password:\n            return None, self.fail(\n                'enable 비밀번호 없음',\n                message='connection credential에 en_password가 필요합니다.',\n            )\n\n        results = self._run_paramiko_commands([\n            {\n                'command': 'enable',\n                'ignore_prompt': True,\n            },\n            {\n                'command': enable_password,\n                'hide_command': True,\n            },\n        ] + list(command_items))\n\n        failed = [\n            item for item in results\n            if item.get('rc') != 0 and not (item.get('command') == 'enable' and item.get('timed_out'))\n        ]\n        if failed:\n            first = failed[0]\n            display_command = first.get('display_command') or first.get('command')\n            return None, self.fail(\n                '점검 명령 실행 실패',\n                message=f'{display_command} 명령 실행에 실패했습니다.',\n                stdout=(first.get('stdout') or '').strip(),\n                stderr=(first.get('stderr') or '').strip(),\n            )\n\n        if len(results) < 2:\n            return None, self.fail(\n                'enable 모드 진입 실패',\n                message='enable 명령 처리 결과가 부족합니다.',\n            )\n\n        enable_prompt = str(results[1].get('prompt') or '').strip()\n        if not enable_prompt.endswith('#'):\n            return None, self.fail(\n                'enable 모드 진입 실패',\n                message='enable 비밀번호 입력 후 privileged prompt(#)를 확인하지 못했습니다.',\n                stdout=(results[1].get('stdout') or '').strip(),\n                stderr=(results[1].get('stderr') or '').strip(),\n            )\n\n        return results, None\n\n    def run(self):\n        results, error = self._run_with_enable([\n            {\n                'command': 'terminal length 0',\n            },\n            {\n                'command': HOSTNAME_COMMAND,\n            },\n        ])\n        if error:\n            return error\n\n        hostname_output = (results[3].get('stdout') or '').strip()\n        hostname_line = next(\n            (line.strip() for line in hostname_output.splitlines() if line.strip().startswith('hostname ')),\n            '',\n        )\n        if not hostname_line:\n            return self.fail(\n                'hostname 출력 없음',\n                message='show running-config | include ^hostname 결과에서 hostname 라인을 찾지 못했습니다.',\n                stdout=hostname_output,\n            )\n\n        hostname = hostname_line.split(None, 1)[1].strip()\n\n        return self.ok(\n            metrics={\n                'commands': [item.get('display_command') or item.get('command') for item in results],\n                'hostname': hostname,\n            },\n            thresholds={},\n            reasons='실행 중 설정의 hostname 값을 정상 수집했습니다.',\n            message=f'hostname 파싱 예제가 정상 수행되었습니다. hostname={hostname}',\n        )\n\n\nCHECK_CLASS = Check\n",
      "check_script_lines": [
        "# -*- coding: utf-8 -*-",
        "",
        "from .common._base import BaseCheck",
        "",
        "",
        "HOSTNAME_COMMAND = 'show running-config | include ^hostname'",
        "",
        "",
        "class Check(BaseCheck):",
        "    USE_HOST_CONNECTION = True",
        "    CONNECTION_METHOD = 'paramiko'",
        "    PARAMIKO_PROFILE = 'cisco_ios'",
        "",
        "    def _run_with_enable(self, command_items):",
        "        enable_password = str(self.get_connection_value('en_password', '') or '')",
        "        if not enable_password:",
        "            return None, self.fail(",
        "                'enable 비밀번호 없음',",
        "                message='connection credential에 en_password가 필요합니다.',",
        "            )",
        "",
        "        results = self._run_paramiko_commands([",
        "            {",
        "                'command': 'enable',",
        "                'ignore_prompt': True,",
        "            },",
        "            {",
        "                'command': enable_password,",
        "                'hide_command': True,",
        "            },",
        "        ] + list(command_items))",
        "",
        "        failed = [",
        "            item for item in results",
        "            if item.get('rc') != 0 and not (item.get('command') == 'enable' and item.get('timed_out'))",
        "        ]",
        "        if failed:",
        "            first = failed[0]",
        "            display_command = first.get('display_command') or first.get('command')",
        "            return None, self.fail(",
        "                '점검 명령 실행 실패',",
        "                message=f'{display_command} 명령 실행에 실패했습니다.',",
        "                stdout=(first.get('stdout') or '').strip(),",
        "                stderr=(first.get('stderr') or '').strip(),",
        "            )",
        "",
        "        if len(results) < 2:",
        "            return None, self.fail(",
        "                'enable 모드 진입 실패',",
        "                message='enable 명령 처리 결과가 부족합니다.',",
        "            )",
        "",
        "        enable_prompt = str(results[1].get('prompt') or '').strip()",
        "        if not enable_prompt.endswith('#'):",
        "            return None, self.fail(",
        "                'enable 모드 진입 실패',",
        "                message='enable 비밀번호 입력 후 privileged prompt(#)를 확인하지 못했습니다.',",
        "                stdout=(results[1].get('stdout') or '').strip(),",
        "                stderr=(results[1].get('stderr') or '').strip(),",
        "            )",
        "",
        "        return results, None",
        "",
        "    def run(self):",
        "        results, error = self._run_with_enable([",
        "            {",
        "                'command': 'terminal length 0',",
        "            },",
        "            {",
        "                'command': HOSTNAME_COMMAND,",
        "            },",
        "        ])",
        "        if error:",
        "            return error",
        "",
        "        hostname_output = (results[3].get('stdout') or '').strip()",
        "        hostname_line = next(",
        "            (line.strip() for line in hostname_output.splitlines() if line.strip().startswith('hostname ')),",
        "            '',",
        "        )",
        "        if not hostname_line:",
        "            return self.fail(",
        "                'hostname 출력 없음',",
        "                message='show running-config | include ^hostname 결과에서 hostname 라인을 찾지 못했습니다.',",
        "                stdout=hostname_output,",
        "            )",
        "",
        "        hostname = hostname_line.split(None, 1)[1].strip()",
        "",
        "        return self.ok(",
        "            metrics={",
        "                'commands': [item.get('display_command') or item.get('command') for item in results],",
        "                'hostname': hostname,",
        "            },",
        "            thresholds={},",
        "            reasons='실행 중 설정의 hostname 값을 정상 수집했습니다.',",
        "            message=f'hostname 파싱 예제가 정상 수행되었습니다. hostname={hostname}',",
        "        )",
        "",
        "",
        "CHECK_CLASS = Check"
      ]
    }
  ],
  "results": [
    {
      "inspection_code": "TUTORIAL-CISCO-PARAMIKO-04",
      "item_id": 98304,
      "application_id": 80300,
      "application_type_id": 30,
      "application_type_name": "NETWORK",
      "application_name": "CISCO_IOS",
      "application_family_name": "CISCO_IOS",
      "threshold_list": [],
      "status": "ok",
      "metrics": {
        "commands": [
          "enable",
          "*******",
          "terminal length 0",
          "show running-config | include ^hostname"
        ],
        "hostname": "TEST_CISCO_C3650"
      },
      "thresholds": {},
      "reasons": "실행 중 설정의 hostname 값을 정상 수집했습니다.",
      "raw_output": "[점검 단계 1]\n - 실행 명령어: enable\n - 명령 종료코드: rc=0 (정상 종료)\n\n[점검 단계 2]\n - 실행 명령어: *******\n - 명령 종료코드: rc=0 (정상 종료)\n - 출력 내용: % Bad IP address or host name\n% Unknown command or computer name, or unable to find computer address\n\n[점검 단계 3]\n - 실행 명령어: terminal length 0\n - 명령 종료코드: rc=0 (정상 종료)\n\n[점검 단계 4]\n - 실행 명령어: show running-config | include ^hostname\n - 명령 종료코드: rc=0 (정상 종료)\n - 출력 내용: hostname TEST_CISCO_C3650",
      "raw_output_lines": [
        "[점검 단계 1]",
        " - 실행 명령어: enable",
        " - 명령 종료코드: rc=0 (정상 종료)",
        "",
        "[점검 단계 2]",
        " - 실행 명령어: *******",
        " - 명령 종료코드: rc=0 (정상 종료)",
        " - 출력 내용: % Bad IP address or host name",
        "% Unknown command or computer name, or unable to find computer address",
        "",
        "[점검 단계 3]",
        " - 실행 명령어: terminal length 0",
        " - 명령 종료코드: rc=0 (정상 종료)",
        "",
        "[점검 단계 4]",
        " - 실행 명령어: show running-config | include ^hostname",
        " - 명령 종료코드: rc=0 (정상 종료)",
        " - 출력 내용: hostname TEST_CISCO_C3650"
      ],
      "message": "hostname 파싱 예제가 정상 수행되었습니다. hostname=TEST_CISCO_C3650"
    }
  ],
  "failed_items": []
}
