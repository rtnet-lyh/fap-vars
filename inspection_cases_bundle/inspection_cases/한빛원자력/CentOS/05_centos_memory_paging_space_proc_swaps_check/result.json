{
  "items": [
    {
      "inspection_code": "U-REPLAY-CENTOS7-PAGING-SPACE-01",
      "item_id": 93025,
      "application_id": 301,
      "application_type_id": 10,
      "application_type_name": "LINUX",
      "application_name": "CentOS7",
      "application_family_name": "Server",
      "threshold_list": [
        {
          "name": "max_paging_space_percent",
          "value1": "50"
        },
        {
          "name": "expected_swap_configured",
          "value1": "true"
        }
      ],
      "check_script": "# -*- coding: utf-8 -*-\n\nimport re\n\nfrom items.common._base import BaseCheck\n\n\nCHECK_COMMAND = 'cat /proc/swaps'\n\n\nclass Check(BaseCheck):\n    USE_HOST_CONNECTION = True\n    CONNECTION_METHOD = 'ssh'\n\n\n    def parse_output(self, output):\n        total = 0.0\n        used = 0.0\n        for line in (output or '').splitlines():\n            parts = line.split()\n            if len(parts) < 5 or parts[0].lower() == 'filename':\n                continue\n            try:\n                total += float(parts[2])\n                used += float(parts[3])\n            except Exception:\n                return {'failure_type': 'parse_failure', 'reason': 'swap size/used value is not numeric.'}\n        if total <= 0:\n            return {'swap_configured': False, 'paging_space_percent': 0.0}\n        return {'swap_configured': True, 'paging_space_percent': round(used / total * 100.0, 1)}\n\n    def evaluate(self, metrics, max_paging_space_percent, expected_swap_configured):\n        if metrics.get('failure_type'):\n            return 'fail'\n        if bool(metrics['swap_configured']) != bool(expected_swap_configured):\n            return 'fail'\n        if metrics['paging_space_percent'] > max_paging_space_percent:\n            return 'fail'\n        return 'ok'\n\n    def build_result(self, metrics, max_paging_space_percent, expected_swap_configured, status):\n        criteria = 'ok: swap_configured=%s and paging usage <= %.1f%%' % (expected_swap_configured, max_paging_space_percent)\n        if metrics.get('failure_type'):\n            return {'message': 'paging space check failed.', 'results': metrics.get('reason', ''), 'criteria': criteria}\n        results = 'swap_configured=%s, paging_space_percent=%.1f%%' % (metrics['swap_configured'], metrics['paging_space_percent'])\n        message = 'paging space check passed.' if status == 'ok' else 'paging space check failed policy.'\n        return {'message': message, 'results': results, 'criteria': criteria}\n\n    def run(self):\n        max_paging_space_percent = self.get_threshold_var('max_paging_space_percent', default=50.0, value_type='float')\n        expected_swap_configured = self.get_threshold_var('expected_swap_configured', default=True, value_type='bool')\n\n        rc, output, error = self._ssh(CHECK_COMMAND)\n\n        if rc != 0:\n            metrics = {\n                'failure_type': 'command_failure',\n                'rc': rc,\n                'reason': (error or output or 'command failed').strip(),\n            }\n        else:\n            metrics = self.parse_output(output)\n\n        status = self.evaluate(metrics, max_paging_space_percent, expected_swap_configured)\n        result = self.build_result(metrics, max_paging_space_percent, expected_swap_configured, status)\n\n        return self.result(\n            status=status,\n            message=result['message'],\n            metrics=metrics,\n            results=result['results'],\n            criteria=result['criteria'],\n        )\n\n\nCHECK_CLASS = Check\n",
      "check_script_lines": [
        "# -*- coding: utf-8 -*-",
        "",
        "import re",
        "",
        "from items.common._base import BaseCheck",
        "",
        "",
        "CHECK_COMMAND = 'cat /proc/swaps'",
        "",
        "",
        "class Check(BaseCheck):",
        "    USE_HOST_CONNECTION = True",
        "    CONNECTION_METHOD = 'ssh'",
        "",
        "",
        "    def parse_output(self, output):",
        "        total = 0.0",
        "        used = 0.0",
        "        for line in (output or '').splitlines():",
        "            parts = line.split()",
        "            if len(parts) < 5 or parts[0].lower() == 'filename':",
        "                continue",
        "            try:",
        "                total += float(parts[2])",
        "                used += float(parts[3])",
        "            except Exception:",
        "                return {'failure_type': 'parse_failure', 'reason': 'swap size/used value is not numeric.'}",
        "        if total <= 0:",
        "            return {'swap_configured': False, 'paging_space_percent': 0.0}",
        "        return {'swap_configured': True, 'paging_space_percent': round(used / total * 100.0, 1)}",
        "",
        "    def evaluate(self, metrics, max_paging_space_percent, expected_swap_configured):",
        "        if metrics.get('failure_type'):",
        "            return 'fail'",
        "        if bool(metrics['swap_configured']) != bool(expected_swap_configured):",
        "            return 'fail'",
        "        if metrics['paging_space_percent'] > max_paging_space_percent:",
        "            return 'fail'",
        "        return 'ok'",
        "",
        "    def build_result(self, metrics, max_paging_space_percent, expected_swap_configured, status):",
        "        criteria = 'ok: swap_configured=%s and paging usage <= %.1f%%' % (expected_swap_configured, max_paging_space_percent)",
        "        if metrics.get('failure_type'):",
        "            return {'message': 'paging space check failed.', 'results': metrics.get('reason', ''), 'criteria': criteria}",
        "        results = 'swap_configured=%s, paging_space_percent=%.1f%%' % (metrics['swap_configured'], metrics['paging_space_percent'])",
        "        message = 'paging space check passed.' if status == 'ok' else 'paging space check failed policy.'",
        "        return {'message': message, 'results': results, 'criteria': criteria}",
        "",
        "    def run(self):",
        "        max_paging_space_percent = self.get_threshold_var('max_paging_space_percent', default=50.0, value_type='float')",
        "        expected_swap_configured = self.get_threshold_var('expected_swap_configured', default=True, value_type='bool')",
        "",
        "        rc, output, error = self._ssh(CHECK_COMMAND)",
        "",
        "        if rc != 0:",
        "            metrics = {",
        "                'failure_type': 'command_failure',",
        "                'rc': rc,",
        "                'reason': (error or output or 'command failed').strip(),",
        "            }",
        "        else:",
        "            metrics = self.parse_output(output)",
        "",
        "        status = self.evaluate(metrics, max_paging_space_percent, expected_swap_configured)",
        "        result = self.build_result(metrics, max_paging_space_percent, expected_swap_configured, status)",
        "",
        "        return self.result(",
        "            status=status,",
        "            message=result['message'],",
        "            metrics=metrics,",
        "            results=result['results'],",
        "            criteria=result['criteria'],",
        "        )",
        "",
        "",
        "CHECK_CLASS = Check"
      ]
    }
  ],
  "results": [
    {
      "inspection_code": "U-REPLAY-CENTOS7-PAGING-SPACE-01",
      "item_id": 93025,
      "application_id": 301,
      "application_type_id": 10,
      "application_type_name": "LINUX",
      "application_name": "CentOS7",
      "application_family_name": "Server",
      "threshold_list": [
        {
          "name": "max_paging_space_percent",
          "value1": "50"
        },
        {
          "name": "expected_swap_configured",
          "value1": "true"
        }
      ],
      "status": "ok",
      "metrics": {
        "swap_configured": true,
        "paging_space_percent": 0.0
      },
      "thresholds": {},
      "reasons": "",
      "raw_output": "[점검 단계 1]\n실행 명령어:\n  cat /proc/swaps\n실행 결과:\n  Filename                                Type            Size    Used    Priority\n  /dev/dm-1                               partition       2097148 0       -2",
      "raw_output_lines": [
        "[점검 단계 1]",
        "실행 명령어:",
        "  cat /proc/swaps",
        "실행 결과:",
        "  Filename                                Type            Size    Used    Priority",
        "  /dev/dm-1                               partition       2097148 0       -2"
      ],
      "message": "paging space check passed.",
      "results": "swap_configured=True, paging_space_percent=0.0%",
      "criteria": "ok: swap_configured=True and paging usage <= 50.0%",
      "executed_command": [
        "cat /proc/swaps"
      ]
    }
  ],
  "failed_items": []
}
