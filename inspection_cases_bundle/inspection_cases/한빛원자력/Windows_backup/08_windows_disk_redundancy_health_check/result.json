{
  "items": [
    {
      "inspection_code": "W-REPLAY-DISK-HA-01",
      "item_id": 90012,
      "application_id": 208,
      "application_type_id": 20,
      "application_type_name": "WINDOWS",
      "application_name": "A",
      "application_family_name": "A",
      "threshold_list": [
        {
          "name": "require_spare_device",
          "value1": "0"
        },
        {
          "name": "failure_keywords",
          "value1": ""
        }
      ],
      "check_script": "# -*- coding: utf-8 -*-\n\nimport re\n\nfrom .common._base import BaseCheck\n\n\nDISK_HA_COMMAND = \"powershell.exe -NoProfile -Command \\\"$cmd = Get-Command Get-VirtualDisk -ErrorAction SilentlyContinue; if (-not $cmd) { 'NOT_APPLICABLE=Get-VirtualDisk unavailable'; exit 0 }; try { $vds = Get-VirtualDisk -ErrorAction Stop } catch { 'Access is denied'; exit 0 }; if (-not $vds) { 'NOT_APPLICABLE=No virtual disk'; exit 0 }; foreach ($vd in $vds) {\\n  'Name=' + $vd.FriendlyName;\\n  'Resiliency=' + $vd.ResiliencySettingName;\\n  'HealthStatus=' + $vd.HealthStatus;\\n  'OperationalStatus=' + (($vd.OperationalStatus -join ','));\\n  ''\\n}\\\"\\n\"\n\n\ndef _parse_int(value):\n    return int(str(value).strip())\n\n\n\ndef _prepare_windows_command(command):\n    text = (command or '').strip()\n    prefixes = (\n        'powershell.exe -NoProfile -Command ',\n        'powershell -Command ',\n    )\n    for prefix in prefixes:\n        if text.startswith(prefix):\n            payload = text[len(prefix):].strip()\n            if len(payload) >= 2 and payload[0] == payload[-1] and payload[0] in ('\"', \"'\"):\n                payload = payload[1:-1]\n            payload = payload.replace('\\\\\"', '\"')\n            payload = payload.replace(\"\\\\'\", \"'\")\n            return payload\n    return text\n\nclass Check(BaseCheck):\n    USE_HOST_CONNECTION = True\n    CONNECTION_METHOD = 'winrm'\n    WINRM_SHELL = 'powershell'\n\n\n    def result(self, status, message, metrics, results, criteria, error=None, raw_output=None, stdout=None, stderr=None):\n        reasons = results\n        thresholds = criteria if isinstance(criteria, dict) else {\"criteria\": criteria}\n\n        if status == \"ok\":\n            return self.ok(\n                metrics=metrics,\n                thresholds=thresholds,\n                reasons=reasons,\n                message=message,\n            )\n        if status == \"warn\":\n            return self.warn(\n                metrics=metrics,\n                thresholds=thresholds,\n                reasons=reasons,\n                message=message,\n            )\n        if status == \"excluded\":\n            return self.not_applicable(\n                message=message,\n                raw_output=raw_output or results,\n            )\n        return self.fail(\n            error=error or message,\n            message=message,\n            metrics=metrics,\n            thresholds=thresholds,\n            reasons=reasons,\n            raw_output=raw_output,\n            stdout=stdout,\n            stderr=stderr,\n        )\n\n    def parse_output(self, legacy_result):\n        metrics = dict(legacy_result.get(\"metrics\") or {})\n        metrics[\"_legacy_result\"] = legacy_result\n        return metrics\n\n    def evaluate(self, metrics):\n        legacy_result = metrics.get(\"_legacy_result\", {})\n        status = str(legacy_result.get(\"status\", \"fail\")).strip().lower()\n        if status in (\"ok\", \"warn\", \"fail\", \"excluded\"):\n            return status\n        return \"fail\"\n\n    def build_result(self, metrics, status):\n        legacy_result = metrics.get(\"_legacy_result\", {})\n        final_metrics = dict(metrics)\n        final_metrics.pop(\"_legacy_result\", None)\n\n        return {\n            \"message\": legacy_result.get(\"message\"),\n            \"results\": legacy_result.get(\"reasons\", \"\"),\n            \"criteria\": legacy_result.get(\"thresholds\", {}),\n            \"error\": legacy_result.get(\"error\"),\n            \"raw_output\": legacy_result.get(\"raw_output\"),\n            \"stdout\": legacy_result.get(\"stdout\"),\n            \"stderr\": legacy_result.get(\"stderr\"),\n            \"metrics\": final_metrics,\n        }\n\n    def run(self):\n        legacy_result = self.execute_check()\n        metrics = self.parse_output(legacy_result)\n        status = self.evaluate(metrics)\n        result = self.build_result(metrics, status)\n\n        return self.result(\n            status=status,\n            message=result[\"message\"],\n            metrics=result[\"metrics\"],\n            results=result[\"results\"],\n            criteria=result[\"criteria\"],\n            error=result[\"error\"],\n            raw_output=result[\"raw_output\"],\n            stdout=result[\"stdout\"],\n            stderr=result[\"stderr\"],\n        )\n\n\n    def execute_check(self):\n        require_spare_device = self.get_threshold_var('require_spare_device', default=0, value_type='int')\n        failure_keywords_raw = self.get_threshold_var('failure_keywords', default='', value_type='str')\n\n        rc, out, err = self._run_ps(_prepare_windows_command(DISK_HA_COMMAND))\n        if self._is_connection_error(rc, err):\n            return self.fail('호스트 연결 실패', message=(err or 'WinRM 연결 확인에 실패했습니다.').strip(), stderr=(err or '').strip())\n        text = (out or '').strip()\n        stderr_text = (err or '').strip()\n\n        if self._is_not_applicable(rc, err) or text.startswith('NOT_APPLICABLE='):\n            return self.not_applicable(message='Storage Spaces 또는 가상 디스크 구성이 없어 디스크 HA 점검 대상이 아닙니다.', raw_output=text or stderr_text)\n        if rc != 0:\n            return self.fail('점검 명령 실행 실패', message='Windows 디스크 HA 점검 명령 실행에 실패했습니다.', stdout=text, stderr=stderr_text)\n\n        failure_keywords = [keyword.strip() for keyword in failure_keywords_raw.split(',') if keyword.strip()]\n        matched_failure_keywords = [keyword for keyword in failure_keywords if keyword.lower() in text.lower()]\n        if matched_failure_keywords:\n            return self.fail('디스크 HA 실패 키워드 감지', message='디스크 HA 결과에서 실패 키워드가 확인되었습니다.', stdout=text, stderr=stderr_text)\n\n        entries = []\n        current = {}\n        for raw_line in text.splitlines():\n            line = raw_line.strip()\n            if not line:\n                if current:\n                    entries.append(current)\n                    current = {}\n                continue\n            if '=' not in line:\n                continue\n            key, value = line.split('=', 1)\n            current[key.strip()] = value.strip()\n        if current:\n            entries.append(current)\n\n        if not entries:\n            return self.fail('디스크 HA 파싱 실패', message='가상 디스크 상태 결과를 해석하지 못했습니다.', stdout=text, stderr=stderr_text)\n\n        degraded = [e for e in entries if e.get('HealthStatus', '').lower() not in ('healthy', 'ok') or 'ok' not in e.get('OperationalStatus', 'ok').lower() and 'completed' not in e.get('OperationalStatus', 'ok').lower()]\n        spare_count = sum(1 for e in entries if e.get('Resiliency', '').lower() == 'spare')\n        if require_spare_device and spare_count < require_spare_device:\n            return self.warn(metrics={'virtual_disk_count': len(entries), 'spare_count': spare_count, 'entries': entries, 'matched_failure_keywords': matched_failure_keywords}, thresholds={'require_spare_device': require_spare_device, 'failure_keywords': failure_keywords}, reasons='예비 디스크 수가 기준치 미만입니다.', message='예비 디스크 수가 기준치 미만입니다.')\n        if degraded:\n            return self.fail('디스크 HA 상태 이상 감지', message='HealthStatus 또는 OperationalStatus가 비정상인 가상 디스크가 확인되었습니다.', stdout=text, stderr=stderr_text)\n\n        return self.ok(metrics={'virtual_disk_count': len(entries), 'spare_count': spare_count, 'entries': entries, 'matched_failure_keywords': matched_failure_keywords}, thresholds={'require_spare_device': require_spare_device, 'failure_keywords': failure_keywords}, reasons='가상 디스크 HA 상태가 기준 범위 내입니다.', message=f'Windows 디스크 HA 점검이 정상입니다. 현재 상태: 가상 디스크 {len(entries)}개, spare {spare_count}개.')\n\n\nCHECK_CLASS = Check\n",
      "check_script_lines": [
        "# -*- coding: utf-8 -*-",
        "",
        "import re",
        "",
        "from .common._base import BaseCheck",
        "",
        "",
        "DISK_HA_COMMAND = \"powershell.exe -NoProfile -Command \\\"$cmd = Get-Command Get-VirtualDisk -ErrorAction SilentlyContinue; if (-not $cmd) { 'NOT_APPLICABLE=Get-VirtualDisk unavailable'; exit 0 }; try { $vds = Get-VirtualDisk -ErrorAction Stop } catch { 'Access is denied'; exit 0 }; if (-not $vds) { 'NOT_APPLICABLE=No virtual disk'; exit 0 }; foreach ($vd in $vds) {\\n  'Name=' + $vd.FriendlyName;\\n  'Resiliency=' + $vd.ResiliencySettingName;\\n  'HealthStatus=' + $vd.HealthStatus;\\n  'OperationalStatus=' + (($vd.OperationalStatus -join ','));\\n  ''\\n}\\\"\\n\"",
        "",
        "",
        "def _parse_int(value):",
        "    return int(str(value).strip())",
        "",
        "",
        "",
        "def _prepare_windows_command(command):",
        "    text = (command or '').strip()",
        "    prefixes = (",
        "        'powershell.exe -NoProfile -Command ',",
        "        'powershell -Command ',",
        "    )",
        "    for prefix in prefixes:",
        "        if text.startswith(prefix):",
        "            payload = text[len(prefix):].strip()",
        "            if len(payload) >= 2 and payload[0] == payload[-1] and payload[0] in ('\"', \"'\"):",
        "                payload = payload[1:-1]",
        "            payload = payload.replace('\\\\\"', '\"')",
        "            payload = payload.replace(\"\\\\'\", \"'\")",
        "            return payload",
        "    return text",
        "",
        "class Check(BaseCheck):",
        "    USE_HOST_CONNECTION = True",
        "    CONNECTION_METHOD = 'winrm'",
        "    WINRM_SHELL = 'powershell'",
        "",
        "",
        "    def result(self, status, message, metrics, results, criteria, error=None, raw_output=None, stdout=None, stderr=None):",
        "        reasons = results",
        "        thresholds = criteria if isinstance(criteria, dict) else {\"criteria\": criteria}",
        "",
        "        if status == \"ok\":",
        "            return self.ok(",
        "                metrics=metrics,",
        "                thresholds=thresholds,",
        "                reasons=reasons,",
        "                message=message,",
        "            )",
        "        if status == \"warn\":",
        "            return self.warn(",
        "                metrics=metrics,",
        "                thresholds=thresholds,",
        "                reasons=reasons,",
        "                message=message,",
        "            )",
        "        if status == \"excluded\":",
        "            return self.not_applicable(",
        "                message=message,",
        "                raw_output=raw_output or results,",
        "            )",
        "        return self.fail(",
        "            error=error or message,",
        "            message=message,",
        "            metrics=metrics,",
        "            thresholds=thresholds,",
        "            reasons=reasons,",
        "            raw_output=raw_output,",
        "            stdout=stdout,",
        "            stderr=stderr,",
        "        )",
        "",
        "    def parse_output(self, legacy_result):",
        "        metrics = dict(legacy_result.get(\"metrics\") or {})",
        "        metrics[\"_legacy_result\"] = legacy_result",
        "        return metrics",
        "",
        "    def evaluate(self, metrics):",
        "        legacy_result = metrics.get(\"_legacy_result\", {})",
        "        status = str(legacy_result.get(\"status\", \"fail\")).strip().lower()",
        "        if status in (\"ok\", \"warn\", \"fail\", \"excluded\"):",
        "            return status",
        "        return \"fail\"",
        "",
        "    def build_result(self, metrics, status):",
        "        legacy_result = metrics.get(\"_legacy_result\", {})",
        "        final_metrics = dict(metrics)",
        "        final_metrics.pop(\"_legacy_result\", None)",
        "",
        "        return {",
        "            \"message\": legacy_result.get(\"message\"),",
        "            \"results\": legacy_result.get(\"reasons\", \"\"),",
        "            \"criteria\": legacy_result.get(\"thresholds\", {}),",
        "            \"error\": legacy_result.get(\"error\"),",
        "            \"raw_output\": legacy_result.get(\"raw_output\"),",
        "            \"stdout\": legacy_result.get(\"stdout\"),",
        "            \"stderr\": legacy_result.get(\"stderr\"),",
        "            \"metrics\": final_metrics,",
        "        }",
        "",
        "    def run(self):",
        "        legacy_result = self.execute_check()",
        "        metrics = self.parse_output(legacy_result)",
        "        status = self.evaluate(metrics)",
        "        result = self.build_result(metrics, status)",
        "",
        "        return self.result(",
        "            status=status,",
        "            message=result[\"message\"],",
        "            metrics=result[\"metrics\"],",
        "            results=result[\"results\"],",
        "            criteria=result[\"criteria\"],",
        "            error=result[\"error\"],",
        "            raw_output=result[\"raw_output\"],",
        "            stdout=result[\"stdout\"],",
        "            stderr=result[\"stderr\"],",
        "        )",
        "",
        "",
        "    def execute_check(self):",
        "        require_spare_device = self.get_threshold_var('require_spare_device', default=0, value_type='int')",
        "        failure_keywords_raw = self.get_threshold_var('failure_keywords', default='', value_type='str')",
        "",
        "        rc, out, err = self._run_ps(_prepare_windows_command(DISK_HA_COMMAND))",
        "        if self._is_connection_error(rc, err):",
        "            return self.fail('호스트 연결 실패', message=(err or 'WinRM 연결 확인에 실패했습니다.').strip(), stderr=(err or '').strip())",
        "        text = (out or '').strip()",
        "        stderr_text = (err or '').strip()",
        "",
        "        if self._is_not_applicable(rc, err) or text.startswith('NOT_APPLICABLE='):",
        "            return self.not_applicable(message='Storage Spaces 또는 가상 디스크 구성이 없어 디스크 HA 점검 대상이 아닙니다.', raw_output=text or stderr_text)",
        "        if rc != 0:",
        "            return self.fail('점검 명령 실행 실패', message='Windows 디스크 HA 점검 명령 실행에 실패했습니다.', stdout=text, stderr=stderr_text)",
        "",
        "        failure_keywords = [keyword.strip() for keyword in failure_keywords_raw.split(',') if keyword.strip()]",
        "        matched_failure_keywords = [keyword for keyword in failure_keywords if keyword.lower() in text.lower()]",
        "        if matched_failure_keywords:",
        "            return self.fail('디스크 HA 실패 키워드 감지', message='디스크 HA 결과에서 실패 키워드가 확인되었습니다.', stdout=text, stderr=stderr_text)",
        "",
        "        entries = []",
        "        current = {}",
        "        for raw_line in text.splitlines():",
        "            line = raw_line.strip()",
        "            if not line:",
        "                if current:",
        "                    entries.append(current)",
        "                    current = {}",
        "                continue",
        "            if '=' not in line:",
        "                continue",
        "            key, value = line.split('=', 1)",
        "            current[key.strip()] = value.strip()",
        "        if current:",
        "            entries.append(current)",
        "",
        "        if not entries:",
        "            return self.fail('디스크 HA 파싱 실패', message='가상 디스크 상태 결과를 해석하지 못했습니다.', stdout=text, stderr=stderr_text)",
        "",
        "        degraded = [e for e in entries if e.get('HealthStatus', '').lower() not in ('healthy', 'ok') or 'ok' not in e.get('OperationalStatus', 'ok').lower() and 'completed' not in e.get('OperationalStatus', 'ok').lower()]",
        "        spare_count = sum(1 for e in entries if e.get('Resiliency', '').lower() == 'spare')",
        "        if require_spare_device and spare_count < require_spare_device:",
        "            return self.warn(metrics={'virtual_disk_count': len(entries), 'spare_count': spare_count, 'entries': entries, 'matched_failure_keywords': matched_failure_keywords}, thresholds={'require_spare_device': require_spare_device, 'failure_keywords': failure_keywords}, reasons='예비 디스크 수가 기준치 미만입니다.', message='예비 디스크 수가 기준치 미만입니다.')",
        "        if degraded:",
        "            return self.fail('디스크 HA 상태 이상 감지', message='HealthStatus 또는 OperationalStatus가 비정상인 가상 디스크가 확인되었습니다.', stdout=text, stderr=stderr_text)",
        "",
        "        return self.ok(metrics={'virtual_disk_count': len(entries), 'spare_count': spare_count, 'entries': entries, 'matched_failure_keywords': matched_failure_keywords}, thresholds={'require_spare_device': require_spare_device, 'failure_keywords': failure_keywords}, reasons='가상 디스크 HA 상태가 기준 범위 내입니다.', message=f'Windows 디스크 HA 점검이 정상입니다. 현재 상태: 가상 디스크 {len(entries)}개, spare {spare_count}개.')",
        "",
        "",
        "CHECK_CLASS = Check"
      ]
    }
  ],
  "results": [
    {
      "inspection_code": "W-REPLAY-DISK-HA-01",
      "item_id": 90012,
      "application_id": 208,
      "application_type_id": 20,
      "application_type_name": "WINDOWS",
      "application_name": "A",
      "application_family_name": "A",
      "threshold_list": [
        {
          "name": "require_spare_device",
          "value1": "0"
        },
        {
          "name": "failure_keywords",
          "value1": ""
        }
      ],
      "status": "warn",
      "metrics": {
        "applicable": false
      },
      "thresholds": {},
      "reasons": "Storage Spaces 또는 가상 디스크 구성이 없어 디스크 HA 점검 대상이 아닙니다.",
      "message": "Storage Spaces 또는 가상 디스크 구성이 없어 디스크 HA 점검 대상이 아닙니다.",
      "raw_output": "[점검 단계 1]\n - 실행 명령어: $cmd = Get-Command Get-VirtualDisk -ErrorAction SilentlyContinue; if (-not $cmd) { 'NOT_APPLICABLE=Get-VirtualDisk unavailable'; exit 0 }; try { $vds = Get-VirtualDisk -ErrorAction Stop } catch { 'Access is denied'; exit 0 }; if (-not $vds) { 'NOT_APPLICABLE=No virtual disk'; exit 0 }; foreach ($vd in $vds) {\n  'Name=' + $vd.FriendlyName;\n  'Resiliency=' + $vd.ResiliencySettingName;\n  'HealthStatus=' + $vd.HealthStatus;\n  'OperationalStatus=' + (($vd.OperationalStatus -join ','));\n  ''\n}\n - 명령 종료코드: rc=0 (정상 종료)\n - 출력 내용(stdout): NOT_APPLICABLE=No virtual disk\n - 출력 내용(stderr): #< CLIXML\n<Objs Version=\"1.1.0.1\" xmlns=\"http://schemas.microsoft.com/powershell/2004/04\"><Obj S=\"progress\" RefId=\"0\"><TN RefId=\"0\"><T>System.Management.Automation.PSCustomObject</T><T>System.Object</T></TN><MS><I64 N=\"SourceId\">1</I64><PR N=\"Record\"><AV>Preparing modules for first use.</AV><AI>0</AI><Nil /><PI>-1</PI><PC>-1</PC><T>Completed</T><SR>-1</SR><SD> </SD></PR></MS></Obj><Obj S=\"progress\" RefId=\"1\"><TNRef RefId=\"0\" /><MS><I64 N=\"SourceId\">2</I64><PR N=\"Record\"><AV>Preparing modules for first use.</AV><AI>0</AI><Nil /><PI>-1</PI><PC>-1</PC><T>Completed</T><SR>-1</SR><SD> </SD></PR></MS></Obj><Obj S=\"progress\" RefId=\"2\"><TNRef RefId=\"0\" /><MS><I64 N=\"SourceId\">3</I64><PR N=\"Record\"><AV>Get-VirtualDisk -ErrorAction Stop } catch { 'Access is denied'; exit 0 }; if (-not $vds) { 'NOT_APPLICABLE=No virtual disk'; exit 0 }; foreach ($vd in $vds) {</AV><AI>46175175</AI><Nil /><PI>-1</PI><PC>100</PC><T>Completed</T><SR>0</SR><SD>1/1 completed</SD></PR></MS></Obj></Objs>",
      "raw_output_lines": [
        "[점검 단계 1]",
        " - 실행 명령어: $cmd = Get-Command Get-VirtualDisk -ErrorAction SilentlyContinue; if (-not $cmd) { 'NOT_APPLICABLE=Get-VirtualDisk unavailable'; exit 0 }; try { $vds = Get-VirtualDisk -ErrorAction Stop } catch { 'Access is denied'; exit 0 }; if (-not $vds) { 'NOT_APPLICABLE=No virtual disk'; exit 0 }; foreach ($vd in $vds) {",
        "  'Name=' + $vd.FriendlyName;",
        "  'Resiliency=' + $vd.ResiliencySettingName;",
        "  'HealthStatus=' + $vd.HealthStatus;",
        "  'OperationalStatus=' + (($vd.OperationalStatus -join ','));",
        "  ''",
        "}",
        " - 명령 종료코드: rc=0 (정상 종료)",
        " - 출력 내용(stdout): NOT_APPLICABLE=No virtual disk",
        " - 출력 내용(stderr): #< CLIXML",
        "<Objs Version=\"1.1.0.1\" xmlns=\"http://schemas.microsoft.com/powershell/2004/04\"><Obj S=\"progress\" RefId=\"0\"><TN RefId=\"0\"><T>System.Management.Automation.PSCustomObject</T><T>System.Object</T></TN><MS><I64 N=\"SourceId\">1</I64><PR N=\"Record\"><AV>Preparing modules for first use.</AV><AI>0</AI><Nil /><PI>-1</PI><PC>-1</PC><T>Completed</T><SR>-1</SR><SD> </SD></PR></MS></Obj><Obj S=\"progress\" RefId=\"1\"><TNRef RefId=\"0\" /><MS><I64 N=\"SourceId\">2</I64><PR N=\"Record\"><AV>Preparing modules for first use.</AV><AI>0</AI><Nil /><PI>-1</PI><PC>-1</PC><T>Completed</T><SR>-1</SR><SD> </SD></PR></MS></Obj><Obj S=\"progress\" RefId=\"2\"><TNRef RefId=\"0\" /><MS><I64 N=\"SourceId\">3</I64><PR N=\"Record\"><AV>Get-VirtualDisk -ErrorAction Stop } catch { 'Access is denied'; exit 0 }; if (-not $vds) { 'NOT_APPLICABLE=No virtual disk'; exit 0 }; foreach ($vd in $vds) {</AV><AI>46175175</AI><Nil /><PI>-1</PI><PC>100</PC><T>Completed</T><SR>0</SR><SD>1/1 completed</SD></PR></MS></Obj></Objs>"
      ]
    }
  ],
  "failed_items": []
}
