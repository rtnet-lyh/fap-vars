{
  "items": [
    {
      "inspection_code": "W-REPLAY-INODE-01",
      "item_id": 90015,
      "application_id": 211,
      "application_type_id": 20,
      "application_type_name": "WINDOWS",
      "application_name": "A",
      "application_family_name": "A",
      "threshold_list": [
        {
          "name": "max_iuse_percent",
          "value1": "80.0"
        },
        {
          "name": "failure_keywords",
          "value1": ""
        }
      ],
      "check_script": "# -*- coding: utf-8 -*-\n\nimport json\n\nfrom .common._base import BaseCheck\n\n\nDISK_INODE_COMMAND = \"powershell -Command \\\"Get-Volume | Where-Object DriveLetter | ForEach-Object { fsutil dirty query ($_.DriveLetter + ':') }\\\"\"\n\n\ndef _parse_float(value):\n    return round(float(str(value).strip()), 2)\n\n\ndef _as_list(value):\n    if isinstance(value, list):\n        return value\n    if value in (None, ''):\n        return []\n    return [value]\n\n\n\ndef _prepare_windows_command(command):\n    text = (command or '').strip()\n    prefixes = (\n        'powershell.exe -NoProfile -Command ',\n        'powershell -Command ',\n    )\n    for prefix in prefixes:\n        if text.startswith(prefix):\n            payload = text[len(prefix):].strip()\n            if len(payload) >= 2 and payload[0] == payload[-1] and payload[0] in ('\"', \"'\"):\n                payload = payload[1:-1]\n            payload = payload.replace('\\\\\"', '\"')\n            payload = payload.replace(\"\\\\'\", \"'\")\n            return payload\n    return text\n\nclass Check(BaseCheck):\n    USE_HOST_CONNECTION = True\n    CONNECTION_METHOD = 'winrm'\n    WINRM_SHELL = 'powershell'\n\n\n    def result(self, status, message, metrics, results, criteria, error=None, raw_output=None, stdout=None, stderr=None):\n        reasons = results\n        thresholds = criteria if isinstance(criteria, dict) else {\"criteria\": criteria}\n\n        if status == \"ok\":\n            return self.ok(\n                metrics=metrics,\n                thresholds=thresholds,\n                reasons=reasons,\n                message=message,\n            )\n        if status == \"warn\":\n            return self.warn(\n                metrics=metrics,\n                thresholds=thresholds,\n                reasons=reasons,\n                message=message,\n            )\n        if status == \"excluded\":\n            return self.excluded(\n                message=message,\n                raw_output=raw_output or results,\n            )\n        return self.fail(\n            error=error or message,\n            message=message,\n            metrics=metrics,\n            thresholds=thresholds,\n            reasons=reasons,\n            raw_output=raw_output,\n            stdout=stdout,\n            stderr=stderr,\n        )\n\n    def parse_output(self, legacy_result):\n        metrics = dict(legacy_result.get(\"metrics\") or {})\n        metrics[\"_legacy_result\"] = legacy_result\n        return metrics\n\n    def evaluate(self, metrics):\n        legacy_result = metrics.get(\"_legacy_result\", {})\n        status = str(legacy_result.get(\"status\", \"fail\")).strip().lower()\n        if status in (\"ok\", \"warn\", \"fail\", \"excluded\"):\n            return status\n        return \"fail\"\n\n    def build_result(self, metrics, status):\n        legacy_result = metrics.get(\"_legacy_result\", {})\n        final_metrics = dict(metrics)\n        final_metrics.pop(\"_legacy_result\", None)\n\n        return {\n            \"message\": legacy_result.get(\"message\"),\n            \"results\": legacy_result.get(\"reasons\", \"\"),\n            \"criteria\": legacy_result.get(\"thresholds\", {}),\n            \"error\": legacy_result.get(\"error\"),\n            \"raw_output\": legacy_result.get(\"raw_output\"),\n            \"stdout\": legacy_result.get(\"stdout\"),\n            \"stderr\": legacy_result.get(\"stderr\"),\n            \"metrics\": final_metrics,\n        }\n\n    def run(self):\n        legacy_result = self.execute_check()\n        metrics = self.parse_output(legacy_result)\n        status = self.evaluate(metrics)\n        result = self.build_result(metrics, status)\n\n        return self.result(\n            status=status,\n            message=result[\"message\"],\n            metrics=result[\"metrics\"],\n            results=result[\"results\"],\n            criteria=result[\"criteria\"],\n            error=result[\"error\"],\n            raw_output=result[\"raw_output\"],\n            stdout=result[\"stdout\"],\n            stderr=result[\"stderr\"],\n        )\n\n\n    def execute_check(self):\n        max_iuse_percent = self.get_threshold_var('max_iuse_percent', default=80.0, value_type='float')\n        failure_keywords_raw = self.get_threshold_var('failure_keywords', default='', value_type='str')\n\n        rc, out, err = self._run_ps(_prepare_windows_command(DISK_INODE_COMMAND))\n        if self._is_connection_error(rc, err):\n            return self.fail('호스트 연결 실패', message=(err or 'WinRM 연결 확인에 실패했습니다.').strip(), stderr=(err or '').strip())\n        if self._is_not_applicable(rc, err):\n            return self.excluded(message='Windows 볼륨의 inode 사용률은 직접 제공되지 않아 점검에서 제외합니다.', raw_output=(out or err or '').strip())\n        if rc != 0:\n            return self.fail('점검 명령 실행 실패', message='Windows inode 근사 사용률 점검 명령 실행에 실패했습니다.', stdout=(out or '').strip(), stderr=(err or '').strip())\n\n        text = (out or '').strip()\n        if not text:\n            return self.excluded(message='Windows inode 사용률 점검 결과가 비어 있어 제외합니다.', raw_output='')\n\n        failure_keywords = [keyword.strip() for keyword in failure_keywords_raw.split(',') if keyword.strip()]\n        matched_failure_keywords = [keyword for keyword in failure_keywords if keyword.lower() in text.lower()]\n        if matched_failure_keywords:\n            return self.fail('inode 점검 실패 키워드 감지', message='inode 점검 결과에서 실패 키워드가 확인되었습니다.', stdout=text, stderr=(err or '').strip())\n\n        lines = [line.strip() for line in text.splitlines() if line.strip()]\n        dirty_volumes = [line for line in lines if 'is dirty' in line.lower()]\n        metrics = {\n            'volume_count': len(lines),\n            'dirty_volume_count': len(dirty_volumes),\n            'dirty_volumes': dirty_volumes,\n            'max_iuse_percent': 100.0 if dirty_volumes else 0.0,\n            'matched_failure_keywords': matched_failure_keywords,\n        }\n        thresholds = {\n            'max_iuse_percent': max_iuse_percent,\n            'failure_keywords': failure_keywords,\n        }\n        if dirty_volumes:\n            return self.fail('볼륨 Dirty 상태 감지', message='Dirty 상태의 볼륨이 확인되었습니다.', stdout=text, stderr=(err or '').strip())\n        return self.ok(metrics=metrics, thresholds=thresholds, reasons='모든 볼륨이 NOT Dirty 상태입니다.', message='Windows 볼륨 dirty 상태 점검이 정상입니다. 현재 상태: 모든 볼륨이 NOT Dirty입니다.')\n\n\nCHECK_CLASS = Check\n",
      "check_script_lines": [
        "# -*- coding: utf-8 -*-",
        "",
        "import json",
        "",
        "from .common._base import BaseCheck",
        "",
        "",
        "DISK_INODE_COMMAND = \"powershell -Command \\\"Get-Volume | Where-Object DriveLetter | ForEach-Object { fsutil dirty query ($_.DriveLetter + ':') }\\\"\"",
        "",
        "",
        "def _parse_float(value):",
        "    return round(float(str(value).strip()), 2)",
        "",
        "",
        "def _as_list(value):",
        "    if isinstance(value, list):",
        "        return value",
        "    if value in (None, ''):",
        "        return []",
        "    return [value]",
        "",
        "",
        "",
        "def _prepare_windows_command(command):",
        "    text = (command or '').strip()",
        "    prefixes = (",
        "        'powershell.exe -NoProfile -Command ',",
        "        'powershell -Command ',",
        "    )",
        "    for prefix in prefixes:",
        "        if text.startswith(prefix):",
        "            payload = text[len(prefix):].strip()",
        "            if len(payload) >= 2 and payload[0] == payload[-1] and payload[0] in ('\"', \"'\"):",
        "                payload = payload[1:-1]",
        "            payload = payload.replace('\\\\\"', '\"')",
        "            payload = payload.replace(\"\\\\'\", \"'\")",
        "            return payload",
        "    return text",
        "",
        "class Check(BaseCheck):",
        "    USE_HOST_CONNECTION = True",
        "    CONNECTION_METHOD = 'winrm'",
        "    WINRM_SHELL = 'powershell'",
        "",
        "",
        "    def result(self, status, message, metrics, results, criteria, error=None, raw_output=None, stdout=None, stderr=None):",
        "        reasons = results",
        "        thresholds = criteria if isinstance(criteria, dict) else {\"criteria\": criteria}",
        "",
        "        if status == \"ok\":",
        "            return self.ok(",
        "                metrics=metrics,",
        "                thresholds=thresholds,",
        "                reasons=reasons,",
        "                message=message,",
        "            )",
        "        if status == \"warn\":",
        "            return self.warn(",
        "                metrics=metrics,",
        "                thresholds=thresholds,",
        "                reasons=reasons,",
        "                message=message,",
        "            )",
        "        if status == \"excluded\":",
        "            return self.excluded(",
        "                message=message,",
        "                raw_output=raw_output or results,",
        "            )",
        "        return self.fail(",
        "            error=error or message,",
        "            message=message,",
        "            metrics=metrics,",
        "            thresholds=thresholds,",
        "            reasons=reasons,",
        "            raw_output=raw_output,",
        "            stdout=stdout,",
        "            stderr=stderr,",
        "        )",
        "",
        "    def parse_output(self, legacy_result):",
        "        metrics = dict(legacy_result.get(\"metrics\") or {})",
        "        metrics[\"_legacy_result\"] = legacy_result",
        "        return metrics",
        "",
        "    def evaluate(self, metrics):",
        "        legacy_result = metrics.get(\"_legacy_result\", {})",
        "        status = str(legacy_result.get(\"status\", \"fail\")).strip().lower()",
        "        if status in (\"ok\", \"warn\", \"fail\", \"excluded\"):",
        "            return status",
        "        return \"fail\"",
        "",
        "    def build_result(self, metrics, status):",
        "        legacy_result = metrics.get(\"_legacy_result\", {})",
        "        final_metrics = dict(metrics)",
        "        final_metrics.pop(\"_legacy_result\", None)",
        "",
        "        return {",
        "            \"message\": legacy_result.get(\"message\"),",
        "            \"results\": legacy_result.get(\"reasons\", \"\"),",
        "            \"criteria\": legacy_result.get(\"thresholds\", {}),",
        "            \"error\": legacy_result.get(\"error\"),",
        "            \"raw_output\": legacy_result.get(\"raw_output\"),",
        "            \"stdout\": legacy_result.get(\"stdout\"),",
        "            \"stderr\": legacy_result.get(\"stderr\"),",
        "            \"metrics\": final_metrics,",
        "        }",
        "",
        "    def run(self):",
        "        legacy_result = self.execute_check()",
        "        metrics = self.parse_output(legacy_result)",
        "        status = self.evaluate(metrics)",
        "        result = self.build_result(metrics, status)",
        "",
        "        return self.result(",
        "            status=status,",
        "            message=result[\"message\"],",
        "            metrics=result[\"metrics\"],",
        "            results=result[\"results\"],",
        "            criteria=result[\"criteria\"],",
        "            error=result[\"error\"],",
        "            raw_output=result[\"raw_output\"],",
        "            stdout=result[\"stdout\"],",
        "            stderr=result[\"stderr\"],",
        "        )",
        "",
        "",
        "    def execute_check(self):",
        "        max_iuse_percent = self.get_threshold_var('max_iuse_percent', default=80.0, value_type='float')",
        "        failure_keywords_raw = self.get_threshold_var('failure_keywords', default='', value_type='str')",
        "",
        "        rc, out, err = self._run_ps(_prepare_windows_command(DISK_INODE_COMMAND))",
        "        if self._is_connection_error(rc, err):",
        "            return self.fail('호스트 연결 실패', message=(err or 'WinRM 연결 확인에 실패했습니다.').strip(), stderr=(err or '').strip())",
        "        if self._is_not_applicable(rc, err):",
        "            return self.excluded(message='Windows 볼륨의 inode 사용률은 직접 제공되지 않아 점검에서 제외합니다.', raw_output=(out or err or '').strip())",
        "        if rc != 0:",
        "            return self.fail('점검 명령 실행 실패', message='Windows inode 근사 사용률 점검 명령 실행에 실패했습니다.', stdout=(out or '').strip(), stderr=(err or '').strip())",
        "",
        "        text = (out or '').strip()",
        "        if not text:",
        "            return self.excluded(message='Windows inode 사용률 점검 결과가 비어 있어 제외합니다.', raw_output='')",
        "",
        "        failure_keywords = [keyword.strip() for keyword in failure_keywords_raw.split(',') if keyword.strip()]",
        "        matched_failure_keywords = [keyword for keyword in failure_keywords if keyword.lower() in text.lower()]",
        "        if matched_failure_keywords:",
        "            return self.fail('inode 점검 실패 키워드 감지', message='inode 점검 결과에서 실패 키워드가 확인되었습니다.', stdout=text, stderr=(err or '').strip())",
        "",
        "        lines = [line.strip() for line in text.splitlines() if line.strip()]",
        "        dirty_volumes = [line for line in lines if 'is dirty' in line.lower()]",
        "        metrics = {",
        "            'volume_count': len(lines),",
        "            'dirty_volume_count': len(dirty_volumes),",
        "            'dirty_volumes': dirty_volumes,",
        "            'max_iuse_percent': 100.0 if dirty_volumes else 0.0,",
        "            'matched_failure_keywords': matched_failure_keywords,",
        "        }",
        "        thresholds = {",
        "            'max_iuse_percent': max_iuse_percent,",
        "            'failure_keywords': failure_keywords,",
        "        }",
        "        if dirty_volumes:",
        "            return self.fail('볼륨 Dirty 상태 감지', message='Dirty 상태의 볼륨이 확인되었습니다.', stdout=text, stderr=(err or '').strip())",
        "        return self.ok(metrics=metrics, thresholds=thresholds, reasons='모든 볼륨이 NOT Dirty 상태입니다.', message='Windows 볼륨 dirty 상태 점검이 정상입니다. 현재 상태: 모든 볼륨이 NOT Dirty입니다.')",
        "",
        "",
        "CHECK_CLASS = Check"
      ]
    }
  ],
  "results": [
    {
      "inspection_code": "W-REPLAY-INODE-01",
      "item_id": 90015,
      "application_id": 211,
      "application_type_id": 20,
      "application_type_name": "WINDOWS",
      "application_name": "A",
      "application_family_name": "A",
      "threshold_list": [
        {
          "name": "max_iuse_percent",
          "value1": "80.0"
        },
        {
          "name": "failure_keywords",
          "value1": ""
        }
      ],
      "status": "ok",
      "metrics": {
        "volume_count": 2,
        "dirty_volume_count": 0,
        "dirty_volumes": [],
        "max_iuse_percent": 0.0,
        "matched_failure_keywords": []
      },
      "thresholds": {
        "max_iuse_percent": 80.0,
        "failure_keywords": []
      },
      "reasons": "모든 볼륨이 NOT Dirty 상태입니다.",
      "raw_output": "[점검 단계 1]\n - 실행 명령어: Get-Volume | Where-Object DriveLetter | ForEach-Object { fsutil dirty query ($_.DriveLetter + ':') }\n - 명령 종료코드: rc=0 (정상 종료)\n - 출력 내용(stdout): Volume - D: is NOT Dirty\nVolume - C: is NOT Dirty\n - 출력 내용(stderr): #< CLIXML\n<Objs Version=\"1.1.0.1\" xmlns=\"http://schemas.microsoft.com/powershell/2004/04\"><Obj S=\"progress\" RefId=\"0\"><TN RefId=\"0\"><T>System.Management.Automation.PSCustomObject</T><T>System.Object</T></TN><MS><I64 N=\"SourceId\">1</I64><PR N=\"Record\"><AV>Preparing modules for first use.</AV><AI>0</AI><Nil /><PI>-1</PI><PC>-1</PC><T>Completed</T><SR>-1</SR><SD> </SD></PR></MS></Obj><Obj S=\"progress\" RefId=\"1\"><TNRef RefId=\"0\" /><MS><I64 N=\"SourceId\">2</I64><PR N=\"Record\"><AV>Preparing modules for first use.</AV><AI>0</AI><Nil /><PI>-1</PI><PC>-1</PC><T>Completed</T><SR>-1</SR><SD> </SD></PR></MS></Obj><Obj S=\"progress\" RefId=\"2\"><TNRef RefId=\"0\" /><MS><I64 N=\"SourceId\">3</I64><PR N=\"Record\"><AV>Get-Volume | Where-Object DriveLetter | ForEach-Object { fsutil dirty query ($_.DriveLetter + ':') }</AV><AI>1729321166</AI><Nil /><PI>-1</PI><PC>100</PC><T>Completed</T><SR>0</SR><SD>1/1 completed</SD></PR></MS></Obj></Objs>",
      "raw_output_lines": [
        "[점검 단계 1]",
        " - 실행 명령어: Get-Volume | Where-Object DriveLetter | ForEach-Object { fsutil dirty query ($_.DriveLetter + ':') }",
        " - 명령 종료코드: rc=0 (정상 종료)",
        " - 출력 내용(stdout): Volume - D: is NOT Dirty",
        "Volume - C: is NOT Dirty",
        " - 출력 내용(stderr): #< CLIXML",
        "<Objs Version=\"1.1.0.1\" xmlns=\"http://schemas.microsoft.com/powershell/2004/04\"><Obj S=\"progress\" RefId=\"0\"><TN RefId=\"0\"><T>System.Management.Automation.PSCustomObject</T><T>System.Object</T></TN><MS><I64 N=\"SourceId\">1</I64><PR N=\"Record\"><AV>Preparing modules for first use.</AV><AI>0</AI><Nil /><PI>-1</PI><PC>-1</PC><T>Completed</T><SR>-1</SR><SD> </SD></PR></MS></Obj><Obj S=\"progress\" RefId=\"1\"><TNRef RefId=\"0\" /><MS><I64 N=\"SourceId\">2</I64><PR N=\"Record\"><AV>Preparing modules for first use.</AV><AI>0</AI><Nil /><PI>-1</PI><PC>-1</PC><T>Completed</T><SR>-1</SR><SD> </SD></PR></MS></Obj><Obj S=\"progress\" RefId=\"2\"><TNRef RefId=\"0\" /><MS><I64 N=\"SourceId\">3</I64><PR N=\"Record\"><AV>Get-Volume | Where-Object DriveLetter | ForEach-Object { fsutil dirty query ($_.DriveLetter + ':') }</AV><AI>1729321166</AI><Nil /><PI>-1</PI><PC>100</PC><T>Completed</T><SR>0</SR><SD>1/1 completed</SD></PR></MS></Obj></Objs>"
      ],
      "message": "Windows 볼륨 dirty 상태 점검이 정상입니다. 현재 상태: 모든 볼륨이 NOT Dirty입니다."
    }
  ],
  "failed_items": []
}
