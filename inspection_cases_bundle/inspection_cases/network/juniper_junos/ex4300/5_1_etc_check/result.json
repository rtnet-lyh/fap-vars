{
  "items": [
    {
      "inspection_code": "NETWORK-JUNIPER-JUNOS-EX4300-5-1-ETC",
      "item_id": 87013,
      "application_id": 85013,
      "application_type_id": 30,
      "application_type_name": "NETWORK",
      "application_name": "JUNIPER_JUNOS_EX4300",
      "application_family_name": "JUNIPER_JUNOS_EX4300",
      "threshold_list": [],
      "check_script": "# -*- coding: utf-8 -*-\n\nimport re\n\nfrom .common._base import BaseCheck\n\n\nCOMMAND_ERROR_MARKERS = ('syntax error', 'unknown command', 'invalid command', 'unknown keyword', 'missing argument')\nCOMMAND = 'show chassis environment'\n\n\nclass Check(BaseCheck):\n    USE_HOST_CONNECTION = True\n    CONNECTION_METHOD = 'paramiko'\n    PARAMIKO_PROFILE = 'generic_network'\n    PARAMIKO_REUSE_SESSION = True\n\n    def _run_command(self, command):\n        results = self._run_paramiko_commands([command], profile=self.PARAMIKO_PROFILE)\n        if not results:\n            return None, self.fail('점검 명령 실행 실패', message='Paramiko 명령 실행 결과가 비어 있습니다.')\n        result = results[0]\n        stdout = (result.get('stdout') or '').strip()\n        stderr = (result.get('stderr') or '').strip()\n        if result.get('rc') != 0:\n            return None, self.fail('점검 명령 실행 실패', message=f'{command} 명령 실행에 실패했습니다.', stdout=stdout, stderr=stderr)\n        error_text = self._detect_cli_error(stdout, stderr)\n        if error_text:\n            return None, self.fail('점검 명령 실행 실패', message=f'{command} 명령 출력에서 오류가 확인되었습니다: {error_text}', stdout=stdout, stderr=stderr)\n        return stdout, None\n\n    def _detect_cli_error(self, *texts):\n        for text in texts:\n            for line in str(text or '').splitlines():\n                stripped = line.strip()\n                lowered = stripped.lower()\n                if stripped and any(marker in lowered for marker in COMMAND_ERROR_MARKERS):\n                    return stripped\n        return ''\n\n    def _parse_environment_statuses(self, text):\n        statuses = []\n        for line in (text or '').splitlines():\n            stripped = line.rstrip()\n            if not stripped or stripped.startswith('Class ') or set(stripped.strip()) <= {'-'}:\n                continue\n            match = re.match(r'^\\s*(?P<item>.+?)\\s{2,}(?P<status>[A-Za-z][A-Za-z_-]*)(?:\\s{2,}.*)?$', stripped)\n            if not match:\n                continue\n            status = match.group('status')\n            if status.lower() not in ('status', 'measurement'):\n                statuses.append({'item': match.group('item').strip(), 'status': status})\n        return statuses\n\n    def run(self):\n        stdout, error = self._run_command(COMMAND)\n        if error:\n            return error\n\n        statuses = self._parse_environment_statuses(stdout)\n        if not statuses:\n            return self.fail('환경 상태 파싱 실패', message='show chassis environment 출력에서 Status 값을 찾지 못했습니다.', stdout=stdout, thresholds={})\n        bad_statuses = [item for item in statuses if item['status'].upper() != 'OK']\n        metrics = {'status_count': len(statuses), 'bad_statuses': bad_statuses, 'statuses': statuses}\n        if bad_statuses:\n            return self.fail('환경 상태 기준 미달', message=f'OK가 아닌 하드웨어 Status가 {len(bad_statuses)}개 있습니다.', stdout=stdout, metrics=metrics, thresholds={})\n        return self.ok(metrics=metrics, thresholds={}, reasons='하드웨어 Status 값이 모두 OK입니다.', message='전원/FAN 등 환경 상태 점검 정상.')\n\n\nCHECK_CLASS = Check\n",
      "check_script_lines": [
        "# -*- coding: utf-8 -*-",
        "",
        "import re",
        "",
        "from .common._base import BaseCheck",
        "",
        "",
        "COMMAND_ERROR_MARKERS = ('syntax error', 'unknown command', 'invalid command', 'unknown keyword', 'missing argument')",
        "COMMAND = 'show chassis environment'",
        "",
        "",
        "class Check(BaseCheck):",
        "    USE_HOST_CONNECTION = True",
        "    CONNECTION_METHOD = 'paramiko'",
        "    PARAMIKO_PROFILE = 'generic_network'",
        "    PARAMIKO_REUSE_SESSION = True",
        "",
        "    def _run_command(self, command):",
        "        results = self._run_paramiko_commands([command], profile=self.PARAMIKO_PROFILE)",
        "        if not results:",
        "            return None, self.fail('점검 명령 실행 실패', message='Paramiko 명령 실행 결과가 비어 있습니다.')",
        "        result = results[0]",
        "        stdout = (result.get('stdout') or '').strip()",
        "        stderr = (result.get('stderr') or '').strip()",
        "        if result.get('rc') != 0:",
        "            return None, self.fail('점검 명령 실행 실패', message=f'{command} 명령 실행에 실패했습니다.', stdout=stdout, stderr=stderr)",
        "        error_text = self._detect_cli_error(stdout, stderr)",
        "        if error_text:",
        "            return None, self.fail('점검 명령 실행 실패', message=f'{command} 명령 출력에서 오류가 확인되었습니다: {error_text}', stdout=stdout, stderr=stderr)",
        "        return stdout, None",
        "",
        "    def _detect_cli_error(self, *texts):",
        "        for text in texts:",
        "            for line in str(text or '').splitlines():",
        "                stripped = line.strip()",
        "                lowered = stripped.lower()",
        "                if stripped and any(marker in lowered for marker in COMMAND_ERROR_MARKERS):",
        "                    return stripped",
        "        return ''",
        "",
        "    def _parse_environment_statuses(self, text):",
        "        statuses = []",
        "        for line in (text or '').splitlines():",
        "            stripped = line.rstrip()",
        "            if not stripped or stripped.startswith('Class ') or set(stripped.strip()) <= {'-'}:",
        "                continue",
        "            match = re.match(r'^\\s*(?P<item>.+?)\\s{2,}(?P<status>[A-Za-z][A-Za-z_-]*)(?:\\s{2,}.*)?$', stripped)",
        "            if not match:",
        "                continue",
        "            status = match.group('status')",
        "            if status.lower() not in ('status', 'measurement'):",
        "                statuses.append({'item': match.group('item').strip(), 'status': status})",
        "        return statuses",
        "",
        "    def run(self):",
        "        stdout, error = self._run_command(COMMAND)",
        "        if error:",
        "            return error",
        "",
        "        statuses = self._parse_environment_statuses(stdout)",
        "        if not statuses:",
        "            return self.fail('환경 상태 파싱 실패', message='show chassis environment 출력에서 Status 값을 찾지 못했습니다.', stdout=stdout, thresholds={})",
        "        bad_statuses = [item for item in statuses if item['status'].upper() != 'OK']",
        "        metrics = {'status_count': len(statuses), 'bad_statuses': bad_statuses, 'statuses': statuses}",
        "        if bad_statuses:",
        "            return self.fail('환경 상태 기준 미달', message=f'OK가 아닌 하드웨어 Status가 {len(bad_statuses)}개 있습니다.', stdout=stdout, metrics=metrics, thresholds={})",
        "        return self.ok(metrics=metrics, thresholds={}, reasons='하드웨어 Status 값이 모두 OK입니다.', message='전원/FAN 등 환경 상태 점검 정상.')",
        "",
        "",
        "CHECK_CLASS = Check"
      ]
    }
  ],
  "results": [
    {
      "inspection_code": "NETWORK-JUNIPER-JUNOS-EX4300-5-1-ETC",
      "item_id": 87013,
      "application_id": 85013,
      "application_type_id": 30,
      "application_type_name": "NETWORK",
      "application_name": "JUNIPER_JUNOS_EX4300",
      "application_family_name": "JUNIPER_JUNOS_EX4300",
      "threshold_list": [],
      "status": "ok",
      "metrics": {
        "status_count": 11,
        "bad_statuses": [],
        "statuses": [
          {
            "item": "Power FPC 0 Power Supply 0",
            "status": "OK"
          },
          {
            "item": "FPC 0 Power Supply 1",
            "status": "OK"
          },
          {
            "item": "Temp  FPC 0 CPU",
            "status": "OK"
          },
          {
            "item": "FPC 0 NW-PFE",
            "status": "OK"
          },
          {
            "item": "FPC 0 SE-PFE",
            "status": "OK"
          },
          {
            "item": "FPC 0 PHY-4/5",
            "status": "OK"
          },
          {
            "item": "FPC 0 MGMT PHY",
            "status": "OK"
          },
          {
            "item": "Fans  FPC 0 Fan 0",
            "status": "OK"
          },
          {
            "item": "FPC 0 Fan 0 Airflow",
            "status": "OK"
          },
          {
            "item": "FPC 0 Fan 1",
            "status": "OK"
          },
          {
            "item": "FPC 0 Fan 1 Airflow",
            "status": "OK"
          }
        ]
      },
      "thresholds": {},
      "reasons": "하드웨어 Status 값이 모두 OK입니다.",
      "raw_output": "[점검 단계 1]\n - 실행 명령어: show chassis environment\n - 명령 종료코드: rc=0 (정상 종료)\n - 출력 내용: Class Item                           Status     Measurement\nPower FPC 0 Power Supply 0           OK        \n      FPC 0 Power Supply 1           OK        \nTemp  FPC 0 CPU                      OK         49 degrees C / 120 degrees F\n      FPC 0 NW-PFE                   OK         41 degrees C / 105 degrees F\n      FPC 0 SE-PFE                   OK         35 degrees C / 95 degrees F\n      FPC 0 PHY-4/5                  OK         33 degrees C / 91 degrees F\n      FPC 0 MGMT PHY                 OK         30 degrees C / 86 degrees F\nFans  FPC 0 Fan 0                    OK         Spinning at normal speed\n      FPC 0 Fan 0 Airflow            OK         Airflow Out (AFO)\n      FPC 0 Fan 1                    OK         Spinning at normal speed\n      FPC 0 Fan 1 Airflow            OK         Airflow Out (AFO)\n\n{master:0}",
      "raw_output_lines": [
        "[점검 단계 1]",
        " - 실행 명령어: show chassis environment",
        " - 명령 종료코드: rc=0 (정상 종료)",
        " - 출력 내용: Class Item                           Status     Measurement",
        "Power FPC 0 Power Supply 0           OK        ",
        "      FPC 0 Power Supply 1           OK        ",
        "Temp  FPC 0 CPU                      OK         49 degrees C / 120 degrees F",
        "      FPC 0 NW-PFE                   OK         41 degrees C / 105 degrees F",
        "      FPC 0 SE-PFE                   OK         35 degrees C / 95 degrees F",
        "      FPC 0 PHY-4/5                  OK         33 degrees C / 91 degrees F",
        "      FPC 0 MGMT PHY                 OK         30 degrees C / 86 degrees F",
        "Fans  FPC 0 Fan 0                    OK         Spinning at normal speed",
        "      FPC 0 Fan 0 Airflow            OK         Airflow Out (AFO)",
        "      FPC 0 Fan 1                    OK         Spinning at normal speed",
        "      FPC 0 Fan 1 Airflow            OK         Airflow Out (AFO)",
        "",
        "{master:0}"
      ],
      "message": "전원/FAN 등 환경 상태 점검 정상."
    }
  ],
  "failed_items": []
}
