{
  "items": [
    {
      "inspection_code": "NETWORK-CISCO-IOS-C2960X-INTERFACE-STATUS-01",
      "item_id": 98604,
      "application_id": 80601,
      "application_type_id": 30,
      "application_type_name": "NETWORK",
      "application_name": "CISCO_IOS_C2960X",
      "application_family_name": "CISCO_IOS_C2960X",
      "threshold_list": [],
      "check_script": "# -*- coding: utf-8 -*-\n\nimport re\n\nfrom .common._base import BaseCheck\n\n\nCOMMAND = 'show interface status'\nROW_RE = re.compile(\n    r'^(?P<port>\\S+)\\s+(?P<name>.*?)(?P<status>connected|notconnect|disabled|err-disabled|inactive|suspended)'\n    r'\\s+(?P<vlan>\\S+)\\s+(?P<duplex>\\S+)\\s+(?P<speed>\\S+)\\s+(?P<type>.+)$',\n    re.IGNORECASE,\n)\n\n\nclass Check(BaseCheck):\n    USE_HOST_CONNECTION = True\n    CONNECTION_METHOD = 'paramiko'\n    PARAMIKO_PROFILE = 'cisco_ios'\n    PARAMIKO_REUSE_SESSION = True\n\n    def _set_enable_password(self):\n        data = self.get_connection_credential_data()\n        for key in ('en_password', 'become_password'):\n            value = self.get_connection_value(key, None)\n            if value not in (None, ''):\n                if isinstance(data, dict) and not data.get('en_password'):\n                    data['en_password'] = str(value)\n                return True\n            value = self.get_application_credential_value(key, None)\n            if value not in (None, ''):\n                if isinstance(data, dict) and not data.get('en_password'):\n                    data['en_password'] = str(value)\n                return True\n        return False\n\n    def _run_command(self):\n        commands = [\n            {'command': 'terminal length 0'},\n            {'command': COMMAND},\n        ]\n        results = self._run_paramiko_commands(commands, enable_mode=self._set_enable_password())\n        failed = [item for item in results if item.get('rc') != 0]\n        if failed:\n            first = failed[0]\n            command = first.get('display_command') or first.get('command')\n            return None, self.fail(\n                '점검 명령 실행 실패',\n                message=f'{command} 명령 실행에 실패했습니다.',\n                stdout=(first.get('stdout') or '').strip(),\n                stderr=(first.get('stderr') or '').strip(),\n            )\n        return (results[-1].get('stdout') or '').strip(), None\n\n    def _parse_rows(self, text):\n        rows = []\n        for line in (text or '').splitlines():\n            match = ROW_RE.match(line.rstrip())\n            if not match:\n                continue\n            rows.append({\n                'port': match.group('port'),\n                'name': match.group('name').strip(),\n                'status': match.group('status').lower(),\n                'vlan': match.group('vlan'),\n                'duplex': match.group('duplex').lower(),\n                'speed': match.group('speed').lower(),\n                'type': match.group('type').strip(),\n            })\n        return rows\n\n    def run(self):\n        stdout, error = self._run_command()\n        if error:\n            return error\n\n        rows = self._parse_rows(stdout)\n        if not rows:\n            return self.fail(\n                '인터페이스 상태 파싱 실패',\n                message='show interface status 출력에서 포트 상태를 찾지 못했습니다.',\n                stdout=stdout,\n            )\n\n        targets = [row for row in rows if row['name']]\n        bad = []\n        for row in targets:\n            negotiated_bad = row['status'] == 'connected' and (\n                row['duplex'] in ('auto', 'a-auto') or row['speed'] in ('auto', 'a-auto')\n            )\n            if row['status'] != 'connected' or negotiated_bad:\n                bad.append(row)\n\n        metrics = {\n            'interface_count': len(rows),\n            'named_interface_count': len(targets),\n            'bad_interfaces': bad,\n            'named_interfaces': targets,\n        }\n        if bad:\n            return self.warn(\n                metrics=metrics,\n                thresholds={},\n                reasons='이름이 지정된 운영 대상 포트 중 connected 상태가 아닌 포트가 있습니다.',\n                message=f'인터페이스 상태 경고: 비정상 운영 대상 포트 {len(bad)}개.',\n            )\n        return self.ok(\n            metrics=metrics,\n            thresholds={},\n            reasons='이름이 지정된 운영 대상 포트가 모두 connected 상태입니다.',\n            message=f'인터페이스 상태 점검 정상: 운영 대상 포트 {len(targets)}개.',\n        )\n\n\nCHECK_CLASS = Check\n",
      "check_script_lines": [
        "# -*- coding: utf-8 -*-",
        "",
        "import re",
        "",
        "from .common._base import BaseCheck",
        "",
        "",
        "COMMAND = 'show interface status'",
        "ROW_RE = re.compile(",
        "    r'^(?P<port>\\S+)\\s+(?P<name>.*?)(?P<status>connected|notconnect|disabled|err-disabled|inactive|suspended)'",
        "    r'\\s+(?P<vlan>\\S+)\\s+(?P<duplex>\\S+)\\s+(?P<speed>\\S+)\\s+(?P<type>.+)$',",
        "    re.IGNORECASE,",
        ")",
        "",
        "",
        "class Check(BaseCheck):",
        "    USE_HOST_CONNECTION = True",
        "    CONNECTION_METHOD = 'paramiko'",
        "    PARAMIKO_PROFILE = 'cisco_ios'",
        "    PARAMIKO_REUSE_SESSION = True",
        "",
        "    def _set_enable_password(self):",
        "        data = self.get_connection_credential_data()",
        "        for key in ('en_password', 'become_password'):",
        "            value = self.get_connection_value(key, None)",
        "            if value not in (None, ''):",
        "                if isinstance(data, dict) and not data.get('en_password'):",
        "                    data['en_password'] = str(value)",
        "                return True",
        "            value = self.get_application_credential_value(key, None)",
        "            if value not in (None, ''):",
        "                if isinstance(data, dict) and not data.get('en_password'):",
        "                    data['en_password'] = str(value)",
        "                return True",
        "        return False",
        "",
        "    def _run_command(self):",
        "        commands = [",
        "            {'command': 'terminal length 0'},",
        "            {'command': COMMAND},",
        "        ]",
        "        results = self._run_paramiko_commands(commands, enable_mode=self._set_enable_password())",
        "        failed = [item for item in results if item.get('rc') != 0]",
        "        if failed:",
        "            first = failed[0]",
        "            command = first.get('display_command') or first.get('command')",
        "            return None, self.fail(",
        "                '점검 명령 실행 실패',",
        "                message=f'{command} 명령 실행에 실패했습니다.',",
        "                stdout=(first.get('stdout') or '').strip(),",
        "                stderr=(first.get('stderr') or '').strip(),",
        "            )",
        "        return (results[-1].get('stdout') or '').strip(), None",
        "",
        "    def _parse_rows(self, text):",
        "        rows = []",
        "        for line in (text or '').splitlines():",
        "            match = ROW_RE.match(line.rstrip())",
        "            if not match:",
        "                continue",
        "            rows.append({",
        "                'port': match.group('port'),",
        "                'name': match.group('name').strip(),",
        "                'status': match.group('status').lower(),",
        "                'vlan': match.group('vlan'),",
        "                'duplex': match.group('duplex').lower(),",
        "                'speed': match.group('speed').lower(),",
        "                'type': match.group('type').strip(),",
        "            })",
        "        return rows",
        "",
        "    def run(self):",
        "        stdout, error = self._run_command()",
        "        if error:",
        "            return error",
        "",
        "        rows = self._parse_rows(stdout)",
        "        if not rows:",
        "            return self.fail(",
        "                '인터페이스 상태 파싱 실패',",
        "                message='show interface status 출력에서 포트 상태를 찾지 못했습니다.',",
        "                stdout=stdout,",
        "            )",
        "",
        "        targets = [row for row in rows if row['name']]",
        "        bad = []",
        "        for row in targets:",
        "            negotiated_bad = row['status'] == 'connected' and (",
        "                row['duplex'] in ('auto', 'a-auto') or row['speed'] in ('auto', 'a-auto')",
        "            )",
        "            if row['status'] != 'connected' or negotiated_bad:",
        "                bad.append(row)",
        "",
        "        metrics = {",
        "            'interface_count': len(rows),",
        "            'named_interface_count': len(targets),",
        "            'bad_interfaces': bad,",
        "            'named_interfaces': targets,",
        "        }",
        "        if bad:",
        "            return self.warn(",
        "                metrics=metrics,",
        "                thresholds={},",
        "                reasons='이름이 지정된 운영 대상 포트 중 connected 상태가 아닌 포트가 있습니다.',",
        "                message=f'인터페이스 상태 경고: 비정상 운영 대상 포트 {len(bad)}개.',",
        "            )",
        "        return self.ok(",
        "            metrics=metrics,",
        "            thresholds={},",
        "            reasons='이름이 지정된 운영 대상 포트가 모두 connected 상태입니다.',",
        "            message=f'인터페이스 상태 점검 정상: 운영 대상 포트 {len(targets)}개.',",
        "        )",
        "",
        "",
        "CHECK_CLASS = Check"
      ]
    }
  ],
  "results": [
    {
      "inspection_code": "NETWORK-CISCO-IOS-C2960X-INTERFACE-STATUS-01",
      "item_id": 98604,
      "application_id": 80601,
      "application_type_id": 30,
      "application_type_name": "NETWORK",
      "application_name": "CISCO_IOS_C2960X",
      "application_family_name": "CISCO_IOS_C2960X",
      "threshold_list": [],
      "status": "warn",
      "metrics": {
        "interface_count": 4,
        "named_interface_count": 2,
        "bad_interfaces": [
          {
            "port": "Fa0/1/2",
            "name": "230515_SONGAGI_1",
            "status": "disabled",
            "vlan": "1",
            "duplex": "auto",
            "speed": "auto",
            "type": "10/100BaseTX"
          },
          {
            "port": "Fa0/1/3",
            "name": "230515_SONGAGI_2",
            "status": "disabled",
            "vlan": "1",
            "duplex": "auto",
            "speed": "auto",
            "type": "10/100BaseTX"
          }
        ],
        "named_interfaces": [
          {
            "port": "Fa0/1/2",
            "name": "230515_SONGAGI_1",
            "status": "disabled",
            "vlan": "1",
            "duplex": "auto",
            "speed": "auto",
            "type": "10/100BaseTX"
          },
          {
            "port": "Fa0/1/3",
            "name": "230515_SONGAGI_2",
            "status": "disabled",
            "vlan": "1",
            "duplex": "auto",
            "speed": "auto",
            "type": "10/100BaseTX"
          }
        ]
      },
      "thresholds": {},
      "reasons": "이름이 지정된 운영 대상 포트 중 connected 상태가 아닌 포트가 있습니다.",
      "message": "인터페이스 상태 경고: 비정상 운영 대상 포트 2개.",
      "raw_output": "[점검 단계 1]\n - 실행 명령어: terminal length 0\n - 명령 종료코드: rc=0 (정상 종료)\n\n[점검 단계 2]\n - 실행 명령어: show interface status\n - 명령 종료코드: rc=0 (정상 종료)\n - 출력 내용: Port    Name               Status       Vlan       Duplex Speed Type\nFa0/1/0                    disabled     10           auto     100 10/100BaseTX\nFa0/1/1                    disabled     11           auto    auto 10/100BaseTX\nFa0/1/2 230515_SONGAGI_1   disabled     1            auto    auto 10/100BaseTX\nFa0/1/3 230515_SONGAGI_2   disabled     1            auto    auto 10/100BaseTX",
      "raw_output_lines": [
        "[점검 단계 1]",
        " - 실행 명령어: terminal length 0",
        " - 명령 종료코드: rc=0 (정상 종료)",
        "",
        "[점검 단계 2]",
        " - 실행 명령어: show interface status",
        " - 명령 종료코드: rc=0 (정상 종료)",
        " - 출력 내용: Port    Name               Status       Vlan       Duplex Speed Type",
        "Fa0/1/0                    disabled     10           auto     100 10/100BaseTX",
        "Fa0/1/1                    disabled     11           auto    auto 10/100BaseTX",
        "Fa0/1/2 230515_SONGAGI_1   disabled     1            auto    auto 10/100BaseTX",
        "Fa0/1/3 230515_SONGAGI_2   disabled     1            auto    auto 10/100BaseTX"
      ],
      "results": [
        {
          "step": 1,
          "command": "terminal length 0",
          "result": ""
        },
        {
          "step": 2,
          "command": "show interface status",
          "result": "Port    Name               Status       Vlan       Duplex Speed Type\nFa0/1/0                    disabled     10           auto     100 10/100BaseTX\nFa0/1/1                    disabled     11           auto    auto 10/100BaseTX\nFa0/1/2 230515_SONGAGI_1   disabled     1            auto    auto 10/100BaseTX\nFa0/1/3 230515_SONGAGI_2   disabled     1            auto    auto 10/100BaseTX"
        }
      ]
    }
  ],
  "failed_items": []
}
