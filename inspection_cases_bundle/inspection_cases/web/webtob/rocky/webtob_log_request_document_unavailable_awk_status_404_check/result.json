{
  "items": [
    {
      "inspection_code": "WEBTOB-ROCKY-REPLAY-013",
      "item_id": 91013,
      "application_id": 201,
      "application_type_id": 10,
      "application_type_name": "LINUX",
      "application_name": "WebtoB",
      "application_family_name": "WebtoB",
      "host_vars": {},
      "threshold_list": [
        {
          "name": "access_log_path",
          "value1": "/home/exTMS/tmax/webtob/log/main"
        },
        {
          "name": "warning_status_code",
          "value1": "404"
        }
      ],
      "check_script": "# -*- coding: utf-8 -*-\n\nfrom .common._base import BaseCheck\n\n\nclass Check(BaseCheck):\n    USE_HOST_CONNECTION = True\n    CONNECTION_METHOD = 'paramiko'\n    PARAMIKO_PROFILE = 'linux'\n    PARAMIKO_REUSE_SESSION = False\n\n    DEFAULT_ACCESS_LOG_PATH = '/home/exTMS/tmax/webtob/log/main'\n    DEFAULT_WARNING_STATUS_CODE = 404\n    COMMAND_TIMEOUT = 10\n\n    def _build_command(self, access_log_path, status_code):\n        path = access_log_path.rstrip('/')\n        return \"awk '$(NF-2)==%s' $(ls %s/access.log*|sort|tail -n 1) | tail -20\" % (\n            status_code,\n            path,\n        )\n\n    def _parse_status_lines(self, stdout, status_code):\n        matches = []\n        for line in str(stdout or '').splitlines():\n            parts = line.split()\n            if len(parts) < 3:\n                continue\n            if parts[-3] == str(status_code):\n                matches.append(line.strip())\n        return matches\n\n    def run(self):\n        access_log_path = str(\n            self.get_threshold_var('access_log_path', default=self.DEFAULT_ACCESS_LOG_PATH, value_type='str') or ''\n        ).strip() or self.DEFAULT_ACCESS_LOG_PATH\n        status_code = self.get_threshold_var(\n            'warning_status_code',\n            default=self.DEFAULT_WARNING_STATUS_CODE,\n            value_type='int',\n        )\n        command = self._build_command(access_log_path, status_code)\n\n        result = self._run_paramiko_commands(\n            [{'command': command, 'timeout': self.COMMAND_TIMEOUT}],\n            become=True,\n            profile='linux',\n        )[0]\n\n        stdout = (result.get('stdout') or '').strip()\n        stderr = (result.get('stderr') or '').strip()\n        if result.get('rc') != 0:\n            return self.fail(\n                'access log 명령 실행 실패',\n                message='WebtoB access log에서 404 응답을 확인하지 못했습니다.',\n                stdout=stdout,\n                stderr=stderr,\n            )\n\n        matches = self._parse_status_lines(stdout, status_code)\n        metrics = {\n            'access_log_path': access_log_path,\n            'warning_status_code': status_code,\n            'matching_status_count': len(matches),\n            'sample_lines': matches[:20],\n        }\n        thresholds = {\n            'access_log_path': access_log_path,\n            'warning_status_code': status_code,\n        }\n\n        if matches:\n            return self.warn(\n                metrics=metrics,\n                thresholds=thresholds,\n                reasons='최신 access log에서 HTTP 404 응답이 발견되었습니다.',\n                message='WebtoB access log 404 응답 경고: count=%s' % len(matches),\n            )\n\n        return self.ok(\n            metrics=metrics,\n            thresholds=thresholds,\n            reasons='최신 access log에서 HTTP 404 응답이 발견되지 않았습니다.',\n            message='WebtoB access log 404 응답 정상: status=%s 미검출' % status_code,\n        )\n\n\nCHECK_CLASS = Check\n",
      "check_script_lines": [
        "# -*- coding: utf-8 -*-",
        "",
        "from .common._base import BaseCheck",
        "",
        "",
        "class Check(BaseCheck):",
        "    USE_HOST_CONNECTION = True",
        "    CONNECTION_METHOD = 'paramiko'",
        "    PARAMIKO_PROFILE = 'linux'",
        "    PARAMIKO_REUSE_SESSION = False",
        "",
        "    DEFAULT_ACCESS_LOG_PATH = '/home/exTMS/tmax/webtob/log/main'",
        "    DEFAULT_WARNING_STATUS_CODE = 404",
        "    COMMAND_TIMEOUT = 10",
        "",
        "    def _build_command(self, access_log_path, status_code):",
        "        path = access_log_path.rstrip('/')",
        "        return \"awk '$(NF-2)==%s' $(ls %s/access.log*|sort|tail -n 1) | tail -20\" % (",
        "            status_code,",
        "            path,",
        "        )",
        "",
        "    def _parse_status_lines(self, stdout, status_code):",
        "        matches = []",
        "        for line in str(stdout or '').splitlines():",
        "            parts = line.split()",
        "            if len(parts) < 3:",
        "                continue",
        "            if parts[-3] == str(status_code):",
        "                matches.append(line.strip())",
        "        return matches",
        "",
        "    def run(self):",
        "        access_log_path = str(",
        "            self.get_threshold_var('access_log_path', default=self.DEFAULT_ACCESS_LOG_PATH, value_type='str') or ''",
        "        ).strip() or self.DEFAULT_ACCESS_LOG_PATH",
        "        status_code = self.get_threshold_var(",
        "            'warning_status_code',",
        "            default=self.DEFAULT_WARNING_STATUS_CODE,",
        "            value_type='int',",
        "        )",
        "        command = self._build_command(access_log_path, status_code)",
        "",
        "        result = self._run_paramiko_commands(",
        "            [{'command': command, 'timeout': self.COMMAND_TIMEOUT}],",
        "            become=True,",
        "            profile='linux',",
        "        )[0]",
        "",
        "        stdout = (result.get('stdout') or '').strip()",
        "        stderr = (result.get('stderr') or '').strip()",
        "        if result.get('rc') != 0:",
        "            return self.fail(",
        "                'access log 명령 실행 실패',",
        "                message='WebtoB access log에서 404 응답을 확인하지 못했습니다.',",
        "                stdout=stdout,",
        "                stderr=stderr,",
        "            )",
        "",
        "        matches = self._parse_status_lines(stdout, status_code)",
        "        metrics = {",
        "            'access_log_path': access_log_path,",
        "            'warning_status_code': status_code,",
        "            'matching_status_count': len(matches),",
        "            'sample_lines': matches[:20],",
        "        }",
        "        thresholds = {",
        "            'access_log_path': access_log_path,",
        "            'warning_status_code': status_code,",
        "        }",
        "",
        "        if matches:",
        "            return self.warn(",
        "                metrics=metrics,",
        "                thresholds=thresholds,",
        "                reasons='최신 access log에서 HTTP 404 응답이 발견되었습니다.',",
        "                message='WebtoB access log 404 응답 경고: count=%s' % len(matches),",
        "            )",
        "",
        "        return self.ok(",
        "            metrics=metrics,",
        "            thresholds=thresholds,",
        "            reasons='최신 access log에서 HTTP 404 응답이 발견되지 않았습니다.',",
        "            message='WebtoB access log 404 응답 정상: status=%s 미검출' % status_code,",
        "        )",
        "",
        "",
        "CHECK_CLASS = Check"
      ]
    }
  ],
  "results": [
    {
      "inspection_code": "WEBTOB-ROCKY-REPLAY-013",
      "item_id": 91013,
      "application_id": 201,
      "application_type_id": 10,
      "application_type_name": "LINUX",
      "application_name": "WebtoB",
      "application_family_name": "WebtoB",
      "host_vars": {},
      "threshold_list": [
        {
          "name": "access_log_path",
          "value1": "/home/exTMS/tmax/webtob/log/main"
        },
        {
          "name": "warning_status_code",
          "value1": "404"
        }
      ],
      "status": "warn",
      "metrics": {
        "access_log_path": "/home/exTMS/tmax/webtob/log/main",
        "warning_status_code": 404,
        "matching_status_count": 4,
        "sample_lines": [
          "172.29.41.55 [08/May/2026:17:53:55 +0900] \"POST /getOpinionPopupInfo.do HTTP/1.1\" 404 12 6",
          "172.25.37.142 [08/May/2026:17:53:55 +0900] \"POST /getLtrsInfoList.do HTTP/1.1\" 404 991 18",
          "172.34.41.60 [08/May/2026:17:53:55 +0900] \"POST /getVmsAutoTargetDrfInfo.do HTTP/1.1\" 404 5418 21",
          "172.29.41.55 [08/May/2026:17:53:55 +0900] \"POST /checkSession.do HTTP/1.1\" 404 14 4"
        ]
      },
      "thresholds": {
        "access_log_path": "/home/exTMS/tmax/webtob/log/main",
        "warning_status_code": 404
      },
      "reasons": "최신 access log에서 HTTP 404 응답이 발견되었습니다.",
      "message": "WebtoB access log 404 응답 경고: count=4",
      "raw_output": "[점검 단계 1]\n - 실행 명령어: awk '$(NF-2)==404' $(ls /home/exTMS/tmax/webtob/log/main/access.log*|sort|tail -n 1) | tail -20\n - 명령 종료코드: rc=0 (정상 종료)\n - 출력 내용: 172.29.41.55 [08/May/2026:17:53:55 +0900] \"POST /getOpinionPopupInfo.do HTTP/1.1\" 404 12 6\n172.25.37.142 [08/May/2026:17:53:55 +0900] \"POST /getLtrsInfoList.do HTTP/1.1\" 404 991 18\n172.34.41.60 [08/May/2026:17:53:55 +0900] \"POST /getVmsAutoTargetDrfInfo.do HTTP/1.1\" 404 5418 21\n172.29.41.55 [08/May/2026:17:53:55 +0900] \"POST /checkSession.do HTTP/1.1\" 404 14 4",
      "raw_output_lines": [
        "[점검 단계 1]",
        " - 실행 명령어: awk '$(NF-2)==404' $(ls /home/exTMS/tmax/webtob/log/main/access.log*|sort|tail -n 1) | tail -20",
        " - 명령 종료코드: rc=0 (정상 종료)",
        " - 출력 내용: 172.29.41.55 [08/May/2026:17:53:55 +0900] \"POST /getOpinionPopupInfo.do HTTP/1.1\" 404 12 6",
        "172.25.37.142 [08/May/2026:17:53:55 +0900] \"POST /getLtrsInfoList.do HTTP/1.1\" 404 991 18",
        "172.34.41.60 [08/May/2026:17:53:55 +0900] \"POST /getVmsAutoTargetDrfInfo.do HTTP/1.1\" 404 5418 21",
        "172.29.41.55 [08/May/2026:17:53:55 +0900] \"POST /checkSession.do HTTP/1.1\" 404 14 4"
      ]
    }
  ],
  "failed_items": []
}
