{
  "items": [
    {
      "inspection_code": "WEBTOB-ROCKY-REPLAY-005",
      "item_id": 91005,
      "application_id": 201,
      "application_type_id": 10,
      "application_type_name": "LINUX",
      "application_name": "WebtoB",
      "application_family_name": "WebtoB",
      "host_vars": {},
      "threshold_list": [
        {
          "name": "process_name",
          "value1": "exTMS"
        },
        {
          "name": "max_mem_usage_percent",
          "value1": "70"
        }
      ],
      "check_script": "# -*- coding: utf-8 -*-\n\nimport re\n\nfrom .common._base import BaseCheck\n\n\nclass Check(BaseCheck):\n    USE_HOST_CONNECTION = True\n    CONNECTION_METHOD = 'paramiko'\n    PARAMIKO_PROFILE = 'linux'\n    PARAMIKO_REUSE_SESSION = False\n\n    DEFAULT_PROCESS_NAME = 'exTMS'\n    DEFAULT_MAX_MEM_USAGE_PERCENT = 70.0\n    COMMAND_TIMEOUT = 10\n\n    def _parse_top_rows(self, stdout):\n        header = []\n        rows = []\n        for line in str(stdout or '').splitlines():\n            parts = re.split(r'\\s+', line.strip())\n            if not parts or parts == ['']:\n                continue\n            if 'PID' in parts and '%CPU' in parts and '%MEM' in parts:\n                header = parts\n                continue\n            if not header or len(parts) < len(header):\n                continue\n\n            try:\n                row = {\n                    'pid': parts[header.index('PID')],\n                    'user': parts[header.index('USER')],\n                    'state': parts[header.index('S')],\n                    'cpu_percent': float(parts[header.index('%CPU')]),\n                    'mem_percent': float(parts[header.index('%MEM')]),\n                    'command': parts[header.index('COMMAND')],\n                }\n            except (ValueError, IndexError):\n                continue\n            rows.append(row)\n        return rows\n\n    def run(self):\n        proccess_name = self.get_host_var(key='process_name')\n        if not proccess_name:\n            process_name = self.get_threshold_var(\n                'process_name', \n                default=self.DEFAULT_PROCESS_NAME, \n                value_type='str'\n            ).strip() \n\n        max_mem_usage_percent = self.get_threshold_var(\n            'max_mem_usage_percent',\n            default=self.DEFAULT_MAX_MEM_USAGE_PERCENT,\n            value_type='float',\n        )\n        command = 'top -b -n 1 | egrep \"PID|%s\"' % process_name\n\n        result = self._run_paramiko_commands(\n            [{'command': command, 'timeout': self.COMMAND_TIMEOUT}],\n            become=True,\n            profile='linux',\n        )[0]\n\n        stdout = (result.get('stdout') or '').strip()\n        stderr = (result.get('stderr') or '').strip()\n        if result.get('rc') != 0:\n            return self.fail(\n                'top 명령 실행 실패',\n                message='WEB 프로세스 메모리 사용률을 확인하지 못했습니다.',\n                stdout=stdout,\n                stderr=stderr,\n            )\n\n        rows = self._parse_top_rows(stdout)\n        if not rows:\n            return self.fail(\n                '프로세스 정보 없음',\n                message='top 출력에서 대상 프로세스를 찾지 못했습니다.',\n                stdout=stdout,\n                stderr=stderr,\n            )\n\n        max_row = max(rows, key=lambda row: row['mem_percent'])\n        over_rows = [row for row in rows if row['mem_percent'] > max_mem_usage_percent]\n        metrics = {\n            'process_name': process_name,\n            'process_count': len(rows),\n            'max_mem_usage_percent': max_row['mem_percent'],\n            'max_mem_pid': max_row['pid'],\n            'max_mem_command': max_row['command'],\n            'over_threshold_count': len(over_rows),\n            'processes': rows,\n        }\n        thresholds = {\n            'process_name': process_name,\n            'max_mem_usage_percent': max_mem_usage_percent,\n        }\n\n        if over_rows:\n            return self.warn(\n                metrics=metrics,\n                thresholds=thresholds,\n                reasons='메모리 사용률 기준 초과 프로세스가 있습니다.',\n                message='WEB 프로세스 메모리 사용률 경고: 최대 %.1f%%, 기준 %.1f%%' % (\n                    max_row['mem_percent'],\n                    max_mem_usage_percent,\n                ),\n            )\n\n        return self.ok(\n            metrics=metrics,\n            thresholds=thresholds,\n            reasons='대상 프로세스 메모리 사용률이 기준 이하입니다.',\n            message='WEB 프로세스 메모리 사용률 정상: 최대 %.1f%%, 기준 %.1f%%' % (\n                max_row['mem_percent'],\n                max_mem_usage_percent,\n            ),\n        )\n\n\nCHECK_CLASS = Check\n",
      "check_script_lines": [
        "# -*- coding: utf-8 -*-",
        "",
        "import re",
        "",
        "from .common._base import BaseCheck",
        "",
        "",
        "class Check(BaseCheck):",
        "    USE_HOST_CONNECTION = True",
        "    CONNECTION_METHOD = 'paramiko'",
        "    PARAMIKO_PROFILE = 'linux'",
        "    PARAMIKO_REUSE_SESSION = False",
        "",
        "    DEFAULT_PROCESS_NAME = 'exTMS'",
        "    DEFAULT_MAX_MEM_USAGE_PERCENT = 70.0",
        "    COMMAND_TIMEOUT = 10",
        "",
        "    def _parse_top_rows(self, stdout):",
        "        header = []",
        "        rows = []",
        "        for line in str(stdout or '').splitlines():",
        "            parts = re.split(r'\\s+', line.strip())",
        "            if not parts or parts == ['']:",
        "                continue",
        "            if 'PID' in parts and '%CPU' in parts and '%MEM' in parts:",
        "                header = parts",
        "                continue",
        "            if not header or len(parts) < len(header):",
        "                continue",
        "",
        "            try:",
        "                row = {",
        "                    'pid': parts[header.index('PID')],",
        "                    'user': parts[header.index('USER')],",
        "                    'state': parts[header.index('S')],",
        "                    'cpu_percent': float(parts[header.index('%CPU')]),",
        "                    'mem_percent': float(parts[header.index('%MEM')]),",
        "                    'command': parts[header.index('COMMAND')],",
        "                }",
        "            except (ValueError, IndexError):",
        "                continue",
        "            rows.append(row)",
        "        return rows",
        "",
        "    def run(self):",
        "        proccess_name = self.get_host_var(key='process_name')",
        "        if not proccess_name:",
        "            process_name = self.get_threshold_var(",
        "                'process_name', ",
        "                default=self.DEFAULT_PROCESS_NAME, ",
        "                value_type='str'",
        "            ).strip() ",
        "",
        "        max_mem_usage_percent = self.get_threshold_var(",
        "            'max_mem_usage_percent',",
        "            default=self.DEFAULT_MAX_MEM_USAGE_PERCENT,",
        "            value_type='float',",
        "        )",
        "        command = 'top -b -n 1 | egrep \"PID|%s\"' % process_name",
        "",
        "        result = self._run_paramiko_commands(",
        "            [{'command': command, 'timeout': self.COMMAND_TIMEOUT}],",
        "            become=True,",
        "            profile='linux',",
        "        )[0]",
        "",
        "        stdout = (result.get('stdout') or '').strip()",
        "        stderr = (result.get('stderr') or '').strip()",
        "        if result.get('rc') != 0:",
        "            return self.fail(",
        "                'top 명령 실행 실패',",
        "                message='WEB 프로세스 메모리 사용률을 확인하지 못했습니다.',",
        "                stdout=stdout,",
        "                stderr=stderr,",
        "            )",
        "",
        "        rows = self._parse_top_rows(stdout)",
        "        if not rows:",
        "            return self.fail(",
        "                '프로세스 정보 없음',",
        "                message='top 출력에서 대상 프로세스를 찾지 못했습니다.',",
        "                stdout=stdout,",
        "                stderr=stderr,",
        "            )",
        "",
        "        max_row = max(rows, key=lambda row: row['mem_percent'])",
        "        over_rows = [row for row in rows if row['mem_percent'] > max_mem_usage_percent]",
        "        metrics = {",
        "            'process_name': process_name,",
        "            'process_count': len(rows),",
        "            'max_mem_usage_percent': max_row['mem_percent'],",
        "            'max_mem_pid': max_row['pid'],",
        "            'max_mem_command': max_row['command'],",
        "            'over_threshold_count': len(over_rows),",
        "            'processes': rows,",
        "        }",
        "        thresholds = {",
        "            'process_name': process_name,",
        "            'max_mem_usage_percent': max_mem_usage_percent,",
        "        }",
        "",
        "        if over_rows:",
        "            return self.warn(",
        "                metrics=metrics,",
        "                thresholds=thresholds,",
        "                reasons='메모리 사용률 기준 초과 프로세스가 있습니다.',",
        "                message='WEB 프로세스 메모리 사용률 경고: 최대 %.1f%%, 기준 %.1f%%' % (",
        "                    max_row['mem_percent'],",
        "                    max_mem_usage_percent,",
        "                ),",
        "            )",
        "",
        "        return self.ok(",
        "            metrics=metrics,",
        "            thresholds=thresholds,",
        "            reasons='대상 프로세스 메모리 사용률이 기준 이하입니다.',",
        "            message='WEB 프로세스 메모리 사용률 정상: 최대 %.1f%%, 기준 %.1f%%' % (",
        "                max_row['mem_percent'],",
        "                max_mem_usage_percent,",
        "            ),",
        "        )",
        "",
        "",
        "CHECK_CLASS = Check"
      ]
    }
  ],
  "results": [
    {
      "inspection_code": "WEBTOB-ROCKY-REPLAY-005",
      "item_id": 91005,
      "application_id": 201,
      "application_type_id": 10,
      "application_type_name": "LINUX",
      "application_name": "WebtoB",
      "application_family_name": "WebtoB",
      "host_vars": {},
      "threshold_list": [
        {
          "name": "process_name",
          "value1": "exTMS"
        },
        {
          "name": "max_mem_usage_percent",
          "value1": "70"
        }
      ],
      "status": "ok",
      "metrics": {
        "process_name": "exTMS",
        "process_count": 4,
        "max_mem_usage_percent": 2.3,
        "max_mem_pid": "1709",
        "max_mem_command": "java",
        "over_threshold_count": 0,
        "processes": [
          {
            "pid": "1473",
            "user": "exTMSagn",
            "state": "S",
            "cpu_percent": 0.0,
            "mem_percent": 0.1,
            "command": "systemd"
          },
          {
            "pid": "1506",
            "user": "exTMSagn",
            "state": "S",
            "cpu_percent": 0.0,
            "mem_percent": 0.0,
            "command": "(sd-pam)"
          },
          {
            "pid": "1709",
            "user": "exTMSagn",
            "state": "S",
            "cpu_percent": 0.0,
            "mem_percent": 2.3,
            "command": "java"
          },
          {
            "pid": "2213",
            "user": "exTMSagn",
            "state": "S",
            "cpu_percent": 0.0,
            "mem_percent": 0.0,
            "command": "dbus-daemon"
          }
        ]
      },
      "thresholds": {
        "process_name": "exTMS",
        "max_mem_usage_percent": 70.0
      },
      "reasons": "대상 프로세스 메모리 사용률이 기준 이하입니다.",
      "raw_output": "[점검 단계 1]\n - 실행 명령어: top -b -n 1 | egrep \"PID|exTMS\"\n - 명령 종료코드: rc=0 (정상 종료)\n - 출력 내용: PID USER      PR  NI    VIRT    RES    SHR S  %CPU  %MEM     TIME+ COMMAND\n   1473 exTMSagn  20   0   89856   9876   8244 S   0.0   0.1   0:34.37 systemd\n   1506 exTMSagn  20   0  153700   5588     12 S   0.0   0.0   0:00.00 (sd-pam)\n   1709 exTMSagn  20   0 4751224 365748  18196 S   0.0   2.3  57:26.29 java\n   2213 exTMSagn  20   0   64484   5420   4924 S   0.0   0.0   0:00.00 dbus-daemon",
      "raw_output_lines": [
        "[점검 단계 1]",
        " - 실행 명령어: top -b -n 1 | egrep \"PID|exTMS\"",
        " - 명령 종료코드: rc=0 (정상 종료)",
        " - 출력 내용: PID USER      PR  NI    VIRT    RES    SHR S  %CPU  %MEM     TIME+ COMMAND",
        "   1473 exTMSagn  20   0   89856   9876   8244 S   0.0   0.1   0:34.37 systemd",
        "   1506 exTMSagn  20   0  153700   5588     12 S   0.0   0.0   0:00.00 (sd-pam)",
        "   1709 exTMSagn  20   0 4751224 365748  18196 S   0.0   2.3  57:26.29 java",
        "   2213 exTMSagn  20   0   64484   5420   4924 S   0.0   0.0   0:00.00 dbus-daemon"
      ],
      "message": "WEB 프로세스 메모리 사용률 정상: 최대 2.3%, 기준 70.0%"
    }
  ],
  "failed_items": []
}
